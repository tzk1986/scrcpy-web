# 开发经验记录

> 本记录汇总开发过程中遇到的坑、根因和解决方案，供后续开发参考，避免重复踩坑。

---

## 索引

- [E001 scrcpy v2.4 协议握手字段](#e001-scrcpy-v24-协议握手字段)
- [E002 scrcpy-server JAR 自动删除机制](#e002-scrcpy-server-jar-自动删除机制)
- [E003 scrcpy 编码器选择策略](#e003-scrcpy-编码器选择策略)
- [E004 Git Bash 路径自动转换](#e004-git-bash-路径自动转换)
- [E005 scrcpy 双连接协议](#e005-scrcpy-双连接协议)
- [E006 scid 格式（十六进制非十进制）](#e006-scid-格式十六进制非十进制)
- [E007 协议实现必须对照源码](#e007-协议实现必须对照源码)
- [E008 Windows asyncio 子进程必须用 ProactorEventLoop](#e008-windows-asyncio-子进程必须用-proactoreventloop)
- [E009 Rockchip 硬件编码器崩溃导致视频流中断](#e009-rockchip-硬件编码器崩溃导致视频流中断)
- [E010 坐标映射必须基于视频实际尺寸而非设备物理分辨率](#e010-坐标映射必须基于视频实际尺寸而非设备物理分辨率)
- [E011 WebCodecs AVCC description 构建必须包含 NAL 头](#e011-webcodecs-avcc-description-构建必须包含-nal-头)
- [E012 WebSocket 消息处理器必须在 connect() 前注册](#e012-websocket-消息处理器必须在-connect-前注册)
- [E013 scrcpy max_size 缩放导致坐标映射错位](#e013-scrcpy-max_size-缩放导致坐标映射错位)
- [E014 视频流端到端调试方法论](#e014-视频流端到端调试方法论)
- [E015 scrcpy-server v4.1 升级注意事项](#e015-scrcpy-server-v41-升级注意事项)
- [E016 /proc/net/tcp 地址格式解析](#e016-procnettcp-地址格式解析)
- [E017 dumpsys gfxinfo 获取 FPS 的正确方式](#e017-dumpsys-gfxinfo-获取-fps-的正确方式)
- [E018 scrcpy 协议不支持运行中改码率，自适应只能重启编码器](#e018-scrcpy-协议不支持运行中改码率自适应只能重启编码器)
- [E019 TCP 可达性预检取代 adb connect 超时等待](#e019-tcp-可达性预检取代-adb-connect-超时等待)
- [E020 编码器静止出帧与 IDR 周期实测（RK3566）](#e020-编码器静止出帧与-idr-周期实测rk3566)
- [E021 rk3568 间歇出帧故障与码率档位无关](#e021-rk3568-间歇出帧故障与码率档位无关)
- [E022 视频流每次连接完整启停 scrcpy-server](#e022-视频流每次连接完整启停-scrcpy-server)

---

## E001 scrcpy v2.4 协议握手字段

**日期**：2026-09-09  
**标签**：`scrcpy` `协议` `握手`

### 症状

协议握手后分辨率解析为异常值：`26674x13876`。服务器继续发送 8 字节后连接关闭，无视频数据。

### 根因

错误的协议理解：以为握手只发 dummy byte + 设备名 + 分辨率（4 字节 = 2x uint16）。

**实际 scrcpy v2.4 协议握手顺序**：

| 顺序 | 长度 | 内容 | 格式 |
|------|------|------|------|
| 1 | 1 字节 | Dummy byte | 固定 `0x00` |
| 2 | 64 字节 | 设备名 | UTF-8，null 填充 |
| 3 | 4 字节 | Codec 名称 | ASCII 字符串（如 `"h264"`） |
| 4 | 4 字节 | 宽度 | uint32，大端序 |
| 5 | 4 字节 | 高度 | uint32，大端序 |

之前把 "h264" 的 ASCII 字节（`68 32 36 34`）按 uint16 解析成"分辨率"，得到 26674x13876。

### 正确实现

```python
# 1. Dummy byte
dummy = await reader.readexactly(1)

# 2. 设备名 (64 字节)
device_name = (await reader.readexactly(64)).decode("utf-8").rstrip("\x00")

# 3. Codec 名称 (4 字节 ASCII)
codec = (await reader.readexactly(4)).decode("ascii")  # "h264"

# 4. 宽度 (uint32 大端序)
width = struct.unpack(">I", await reader.readexactly(4))[0]

# 5. 高度 (uint32 大端序)
height = struct.unpack(">I", await reader.readexactly(4))[0]
```

### 经验

- 协议字段顺序不能猜测，必须对照 server 源码（`server/src/main/java/com/genymobile/scrcpy/Streamer.java`）
- 解析出异常数值（如分辨率 26674x13876）时，先检查原始字节是否是 ASCII 字符串

### 参考

- `docs/视频流修复完成.md`
- scrcpy v2.4 源码：`server/src/main/java/com/genymobile/scrcpy/Streamer.java`

---

## E002 scrcpy-server JAR 自动删除机制

**日期**：2026-09-09  
**标签**：`scrcpy` `server` `JAR`

### 症状

scrcpy-server 启动后立即崩溃，`adb shell` 输出 `Aborted`，进程退出码 134（SIGABRT）。

### 根因

scrcpy-server 在退出时（无论正常还是异常）会**自动删除** `/data/local/tmp/scrcpy-server.jar`。

因此每次启动前必须重新推送 JAR，不能假设上次推送的文件还在。

### 解决方案

```python
# 每次启动 server 前必须推送 JAR
async def start_server(self, device_id: str):
    # 1. 先推送 JAR（必须！）
    await self._push_jar(device_id)
    
    # 2. 再启动 server
    await self._run_server(device_id)
```

### 经验

- 设备上的临时文件可能被对方程序自动清理，不能假设"上次推送过就一直在"
- 推送失败时（如 Windows Git Bash 路径转换错误）会静默导致后续崩溃，必须检查推送结果

### 关联

- [E004 Git Bash 路径自动转换](#e004-git-bash-路径自动转换)

---

## E003 scrcpy 编码器选择策略

**日期**：2026-09-09  
**标签**：`scrcpy` `编码器` `设备兼容`

### 症状

Server 启动报错：`Video encoder 'OMX.google.h264.encoder' for h264 not found`。

### 根因

不同 Android 设备支持的硬件编码器不同：
- RK3288 设备：`OMX.rk.video_encoder.avc`
- 通用 Google 软件编码器：`OMX.google.h264.encoder`

硬编码编码器名称会导致在不支持的设备上启动失败。

### 解决方案

**不指定 `video_encoder` 参数**，让 scrcpy-server 自动选择可用的编码器。

```python
cmd = [
    "adb", "-s", device_id, "shell",
    f"CLASSPATH={SCRCPY_SERVER_REMOTE_PATH}",
    "app_process", "/",
    SCRCPY_SERVER_CLASS,
    SCRCPY_SERVER_VERSION,
    "log_level=info",
    f"max_size={opts.max_size}",
    f"max_fps={opts.fps}",
    f"video_bit_rate={bit_rate_value}",
    "video_codec=h264",          # 必须指定编码格式
    # ❌ 不指定 video_encoder，让服务器自动选择
    "tunnel_forward=true",
    "send_frame_meta=false",
    "control=true",
    "audio=false",
    # ...
]
```

### 经验

- 硬件能力因设备而异，不要硬编码硬件相关参数
- `video_codec`（编码格式）可以固定为 `h264`（浏览器友好），但 `video_encoder`（具体编码器实现）让 server 自动选择
- Server 启动日志会打印可用编码器列表，调试时可查看

### 调试命令

```bash
adb shell dumpsys media.codec | grep -i encoder
```

---

## E004 Git Bash 路径自动转换

**日期**：2026-09-09  
**标签**：`Windows` `Git Bash` `ADB` `路径`

### 症状

`adb push local.jar /data/local/tmp/scrcpy-server.jar` 失败，错误：

```
failed to copy to 'C:/Program Files/Git/data/local/tmp/scrcpy-server.jar'
```

或 `adb shell ls /data/local/tmp/` 报错：

```
ls: Files/Git/data/local/tmp/scrcpy-server.jar: not found
```

### 根因

Git Bash（MSYS2）会**自动将 POSIX 风格路径转换为 Windows 路径**：
- `/data/local/tmp` → `C:/Program Files/Git/data/local/tmp`

这对 ADB 命令（目标路径是 Android 设备上的 POSIX 路径）是灾难性的。

### 解决方案

**方法 1**：设置环境变量禁用路径转换

```bash
MSYS_NO_PATHCONV=1 adb push local.jar /data/local/tmp/scrcpy-server.jar
```

**方法 2**：在 `asyncio.create_subprocess_exec` 中传递环境变量

```python
env = os.environ.copy()
env["MSYS_NO_PATHCONV"] = "1"
process = await asyncio.create_subprocess_exec(
    "adb", "-s", device_id, "push", local_path, remote_path,
    env=env,
)
```

**方法 3**：使用 `//` 开头（Git Bash 识别为绝对路径，不转换）

```bash
adb shell ls //data//local//tmp/
```

### 经验

- 在 Windows Git Bash 中执行 ADB 命令**必须**设置 `MSYS_NO_PATHCONV=1`
- 所有涉及 ADB 的 subprocess 调用都要传递这个环境变量
- `adb push/pull`、`adb shell`、`adb forward` 都受影响

---

## E005 scrcpy 双连接协议

**日期**：2026-09-09  
**标签**：`scrcpy` `协议` `连接`

### 症状

只建立视频连接时，server 行为异常或协议不同步。

### 根因

scrcpy v2.x 协议**强制要求建立两个连接**：
1. **视频 socket**：用于接收视频数据
2. **控制 socket**：用于发送控制事件（触摸、按键等）

即使不用控制功能，也必须建立控制连接，否则协议状态不正确。

### 正确实现

```python
# 1. 建立视频连接（先）
video_reader, video_writer = await asyncio.open_connection("127.0.0.1", port)

# 2. 建立控制连接（后）
_, control_writer = await asyncio.open_connection("127.0.0.1", port)

# 两个连接都要保持打开
```

### 经验

- 协议连接顺序很重要：视频连接先建立，控制连接后建立
- "即使不用也要建立"是常见的协议设计模式（如 HTTP/2 的 SETTINGS 帧）
- 停止时必须关闭两个连接，否则会泄漏资源

---

## E006 scid 格式（十六进制非十进制）

**日期**：2026-09-09  
**标签**：`scrcpy` `scid` `格式`

### 症状

scrcpy v4.x server 无法识别 socket 连接，连接被拒绝。

### 根因

scid（Session ID）是 **31 位随机数，格式化为 8 位十六进制零填充**，不是十进制。

scrcpy 源码 `app/src/server.c`：
```c
snprintf(scid_string, sizeof(scid_string), "%08x", scid);  // %08x = 十六进制
```

错误实现：`f"scrcpy_{scid}"`（scid 是十进制）  
正确实现：`f"scrcpy_{scid:08x}"`（scid 格式化为 8 位十六进制）

### 注意

**scrcpy v2.x 使用固定 socket 名称 `"scrcpy"`**，不含 scid。只有 v4.x 才需要 scid。

### 经验

- 格式化的进制（十进制 vs 十六进制）必须对照源码确认
- 版本差异可能导致 socket 命名规则不同

---

## E007 协议实现必须对照源码

**日期**：2026-09-09  
**标签**：`方法论` `调研` `协议`

### 症状

多次修改 scrcpy 协议实现，始终盲改试错，问题反复出现。

### 根因

没有阅读 scrcpy-server 源码，仅凭猜测和文档（可能不准确）实现协议。

### 正确做法

1. **找到源码中的协议实现**：
   - `server/src/main/java/com/genymobile/scrcpy/Server.java` - 启动流程
   - `server/src/main/java/com/genymobile/scrcpy/Streamer.java` - 握手数据发送
   - `app/src/server.c` - socket 命名

2. **对照源码逐字节实现**：
   - 看 server 发送了什么（`writeFully`、`write` 调用）
   - 看 server 期望接收什么（`readFully`、`read` 调用）

3. **参考已验证的实现**：
   - py-scrcpy-client：https://github.com/leng-yue/py-scrcpy-client
   - 但要结合 server 源码验证（py-scrcpy-client 也可能有过期实现）

### 经验

- **禁止盲改试错**（违反 DEVELOPMENT.md 约束 7）
- 协议实现**必须**对照源码
- 文档可能过期，源码才是唯一真实
- 调研→分析→方案→实施→验证 是强制流程

### 关联

- DEVELOPMENT.md 约束 7：问题修复流程约束

---

## E008 Windows asyncio 子进程必须用 ProactorEventLoop

**日期**：2026-09-09  
**标签**：`Windows` `asyncio` `子进程`

### 症状

Windows 上 `asyncio.create_subprocess_exec` 报错：
```
NotImplementedError: subprocesses are not supported on Windows with SelectorEventLoop
```

### 根因

Windows 默认的 `SelectorEventLoop` **不支持子进程**。必须使用 `ProactorEventLoop`。

### 解决方案

在程序入口处（`run_server.py`）设置事件循环策略：

```python
import sys
import asyncio

if sys.platform == "win32":
    asyncio.set_event_loop_policy(asyncio.WindowsProactorEventLoopPolicy())
```

### 经验

- Windows 上任何涉及 `asyncio.create_subprocess_exec` 的程序都必须配置 ProactorEventLoop
- 必须在**主线程**、**程序入口**处设置，不能在子线程或异步函数中设置
- FastAPI/Uvicorn 会在启动时创建 loop，所以要在 `uvicorn.run()` 之前设置

---

## E009 Rockchip 硬件编码器崩溃导致视频流中断

**日期**：2026-09-10
**标签**：`scrcpy` `编码器` `Rockchip` `兼容性`

### 症状

浏览器显示"正在连接..."，始终无法进入"直播中"状态。后端日志显示 scrcpy-server 正常启动并完成协议握手，但只发送了约 21 帧（~150KB）后数据流停止。WebSocket 连接保持打开但不再有数据传输。

### 诊断过程

1. **排除前端问题**：Vite 代理正确转发 WebSocket（JSON config + 二进制帧均通过）
2. **排除后端问题**：直连后端 8765 端口行为一致（初始帧后停止）
3. **编码器诊断日志**：在 `read_socket` 和 yield 循环添加计数，确认数据在 TCP 层就停止了
4. **设备日志**：`adb logcat` 发现关键线索：
   ```
   OMXNodeInstance: !!! Observer died. Quickly, do something, ... anything...
   scrcpy: Cleaning up
   ```
5. **根因确认**：Rockchip RK3288 的 OMX 编码器组件 (OMX.rk.video_encoder.avc) 在编码几秒后崩溃
6. **多种参数测试**：降低分辨率（800x448）、帧率（15fps）、码率（2Mbps）均无效

### 根因

设备硬件编码器 OMX.rk.video_encoder.avc 存在兼容性问题：
- 编码器能成功初始化（setupVideoEncoder succeeded）
- 能产出少量帧（SPS/PPS + IDR + 几帧 P-frame）
- 几秒后 OMX 组件崩溃（Observer died），不再产出帧
- 这是 Android 7.1.2 + Rockchip RK3288 的已知问题

参考：[scrcpy GitHub #2964](https://github.com/Genymobile/scrcpy/issues/2964)

### 解决方案

**前端自动回退**（2026-09-17 升级为 watchdog 四信号，`services/videoFallback.ts`）：

旧实现是单一 `setTimeout` 检查 `frameCount===0`，但有盲区：RK3288 常先成功
解码前几帧（进入 streaming、定时器被取消）再崩溃 → 画面永久冻结、永不回退。

新判据（纯函数，由 1Hz stats 心跳 + 状态变化驱动）：
- `error`：解码器/服务端已报错 → 立即回退
- `stalled`：收到过帧但 `lastFrameTime` 冻结 ≥3s → 回退（覆盖出帧后崩溃，修复盲区）
- `no-stream`：从未出画面且超过 8s → 回退
- `timeout`：15s 硬兜底

```typescript
const r = evaluateH264Fallback({
  state: s.state, frameCount: s.frameCount, lastFrameTime: s.lastFrameTime,
  startedAt, now: Date.now(),
})
if (r.fallback) { h264Stream.stop(); startScreenshotMode() }
```

### 经验

- **编码器崩溃不会报错到客户端**：TCP socket 保持打开，但数据停止流动。需要通过超时检测来发现
- **设备日志是关键**：`adb logcat | grep OMX` 能发现编码器层面的问题
- **硬件兼容性问题无软件解法**：降低参数无效，只能回退到截图模式
- **分层诊断**：前端 → 代理 → 后端 → TCP → 设备，逐层排除

### 诊断命令

```bash
# 检查设备可用编码器
adb shell cat /system/etc/media_codecs.xml | grep -i encoder

# 实时监控编码器日志
adb logcat | grep -E 'OMX|MediaCodec|scrcpy|encoder'

# 检查编码器是否崩溃
adb logcat | grep "Observer died"
```

---

## E010 坐标映射必须基于视频实际尺寸而非设备物理分辨率

**日期**：2026-09-11
**标签**：`坐标映射` `输入控制` `视频流`

### 症状

截图模式下鼠标点击定位不准确，点击位置与实际触发位置有偏差。

### 诊断过程

1. **分析 InputController.mapToScreen() 逻辑**：
   - 使用 `canvas.width/canvas.height` 计算视频纵横比
   - 使用 `this.deviceWidth/deviceHeight`（设备物理分辨率）映射到设备坐标

2. **发现问题**：
   - 截图模式下，`canvas.width/height` 被 `renderBlob` 更新为截图实际尺寸（如 1080x608）
   - 但 `deviceWidth/deviceHeight` 来自 API 的设备物理分辨率（如 1080x1920）
   - 如果设备横屏，截图是 1920x1080，但 deviceWidth=1080, deviceHeight=1920
   - 导致坐标映射错误

### 根因

坐标映射应基于**当前显示的视频/截图实际尺寸**，而不是设备物理分辨率。

参考 scrcpy 源码（`app/src/input_manager.c` + `screen.h`）：
```c
// scrcpy 使用 frame_size（视频帧尺寸）而非设备物理分辨率
*x = *x * screen->frame_size.width / screen->window_size.width;
*y = *y * screen->frame_size.height / screen->window_size.height;
```

### 解决方案

使用 `canvas.width/height`（视频/截图实际像素尺寸）进行坐标映射：

```typescript
mapToScreen(clientX: number, clientY: number, canvas: HTMLCanvasElement) {
    const rect = canvas.getBoundingClientRect()
    const canvasPixelWidth = canvas.width   // 视频/截图实际宽度
    const canvasPixelHeight = canvas.height // 视频/截图实际高度

    // 计算视频显示区域（处理 letterboxing）
    const videoAspect = canvasPixelWidth / canvasPixelHeight
    const containerAspect = rect.width / rect.height

    // ... 计算 displayWidth, displayHeight, offsetX, offsetY ...

    // 计算点击在视频区域内的相对位置（0-1）
    const relX = (clientX - rect.left - offsetX) / displayWidth
    const relY = (clientY - rect.top - offsetY) / displayHeight

    // 映射到设备坐标（基于视频实际尺寸）
    return {
        x: Math.round(relX * canvasPixelWidth),
        y: Math.round(relY * canvasPixelHeight),
    }
}
```

### 经验

- **坐标映射必须基于显示内容的实际尺寸**，而非设备的物理属性
- 设备可能横屏/竖屏，截图尺寸会随之变化
- 参考官方实现（如 scrcpy）的坐标转换逻辑
- 添加边界检查（clamp）防止坐标超出范围

### 参考

- scrcpy 源码：`app/src/input_manager.c`、`app/src/screen.h`
- GitHub 搜索结果确认 scrcpy 使用 `frame_size` 而非设备分辨率

---

## 通用经验提炼

### 1. 协议实现铁律

- **不猜测**：协议字段顺序、长度、进制必须对照源码
- **参考多源**：官方源码 + 已验证客户端实现
- **解析出异常值时**：先检查是否是 ASCII 字符串、是否读错了偏移

### 2. Windows 开发坑

| 坑 | 解决 |
|---|---|
| Git Bash 路径转换 | `MSYS_NO_PATHCONV=1` |
| asyncio 子进程 | `WindowsProactorEventLoopPolicy()` |
| Unicode 输出 | `PYTHONIOENCODING=utf-8` 或避免特殊字符 |

### 3. 资源生命周期

- 对方程序可能自动清理临时文件（如 scrcpy-server 删除 JAR）
- 多连接协议必须全部正确关闭
- 子进程必须在 finally 中 terminate + 检查 returncode

### 4. 调研方法论

症状 → 假设 → **查源码验证假设** → 修复

不是：症状 → 猜测 → 改代码试试 → 再猜 → 再改

### 5. 调试技巧

- 抓原始字节，看十六进制（`data.hex()`）
- 尝试不同解码方式（ASCII、UTF-8、uint16、uint32）
- 对照源码逐字节核对
- 用最小复现脚本隔离问题

---

## E011 WebCodecs AVCC description 构建必须包含 NAL 头

**日期**：2026-09-11  
**标签**：`WebCodecs` `AVCC` `H264` `SPS/PPS`

### 症状

WebCodecs VideoDecoder 报错：`An EncodedVideoChunk was marked as type key but wasn't a key frame`，所有帧解码失败，触发 15 秒超时回退到截图模式。

### 根因

AVCC (avcC box) 格式中 SPS/PPS 数据的正确内容：
- **包含 NAL 头**（如 `0x67 xx xx ...`），只去掉起始码（`00 00 00 01` 或 `00 00 01`）
- 参考 ISO 14496-15 AVC Sample Entry 规范

错误实现：
```typescript
// ❌ 去掉了 NAL 头（0x67），只保留 RBSP
const sps = stripStartCodeAndNalHeader(spsWithStartCode)
```

正确实现：
```typescript
// ✅ 只去起始码，保留 NAL 头
const sps = stripStartCode(spsWithStartCode)  // 保留 0x67 xx xx ...
```

### SPS 字段索引

SPS 数据（保留 NAL 头后）：
```
sps[0] = NAL header (0x67 for SPS)
sps[1] = profile_idc
sps[2] = constraint_set flags (compatibility)
sps[3] = level_idc
```

错误用法 `sps[0]/sps[1]/sps[2]` → 正确用法 `sps[1]/sps[2]/sps[3]`

### AVCC totalSize 计算

```
// ❌ 缺少 numOfPPS 字段（1 字节）
totalSize = 6 + 2 + sps.length + 2 + pps.length  // = 36（RangeError）

// ✅ 正确
totalSize = 7 + 2 + sps.length + 2 + pps.length  // = 37
```

avcC box 结构（7 字节头部）：
```
configurationVersion (1) + profile (1) + compatibility (1) + level (1) +
lengthSizeMinusOne (1) + numOfSPS (1, 高3位=0x1F | 低5位=1) +
SPS_length (2) + SPS_data + numOfPPS (1) +
PPS_length (2) + PPS_data
```

### 经验

- AVCC/avcC 是 ISO 14496-15 定义的格式，必须**严格**按规范实现
- WebCodecs 的 `description` 字段是 ArrayBuffer（avcC 二进制），不是字符串
- 错误的数据格式不一定报错，可能导致解码器状态异常（如"非关键帧被标记为关键帧"）

### 参考

- ISO 14496-15: AVC file format
- WebCodecs VideoDecoder codec string 规范

---

## E012 WebSocket 消息处理器必须在 connect() 前注册

**日期**：2026-09-11  
**标签**：`WebSocket` `时序` `竞态条件`

### 症状

H264 视频流始终不显示，前端 console 无任何 `[H264] Config received` 日志，但有大量 `Frame dropped: decoder not ready, state: undefined decoder exists: false`。

### 根因

消息处理器的注册时序错误：
```typescript
// ❌ 错误：先 connect() 再注册 handler
ws.connect()              // 服务端立即发送 config 消息
h264Stream.start()        // 此时注册 handler → config 消息已被丢弃
```

WebSocket 连接建立后，服务端立即发送 config JSON 消息。如果此时 `onMessage` 回调还未注册，消息会被丢弃（`WebSocketService` 的 `onMessage` 为 null 时直接忽略）。

### 正确实现

```typescript
// ✅ 正确：先注册 handler，再 connect
h264Stream.start()   // 注册 setMessageHandler
ws.connect()         // 连接后 config 消息能正确到达 handler
```

### 经验

- WebSocket 消息可能在连接建立后立即到达，处理器必须提前注册
- 对于"请求-响应"模式（如 config），时序问题尤为关键
- 建议：API 设计上先调用 `start()`（注册 handler），后调用 `connect()`（建立连接）

---

## E013 scrcpy max_size 缩放导致坐标映射错位

**日期**：2026-09-11  
**标签**：`scrcpy` `坐标映射` `max_size` `输入控制`

### 症状

H264 模式下后端日志显示 `sending_touch` 正常触发，但设备无响应或响应位置错误。点击位置整体偏移。

### 根因

`max_size=1080` 导致 scrcpy-server 将视频缩小：
- 设备物理分辨率：1280x720（横屏）
- max_size=1080 缩放后视频：1080x607
- 前端 canvas 尺寸：1080x607（基于视频帧）
- InputController 映射坐标：0-1080（基于 canvas 尺寸）
- scrcpy-server 期望坐标：0-1280（设备物理分辨率）

坐标范围不一致：前端发送 (540, 300)，server 当作 (540, 300) 处理，但实际设备坐标范围是 0-1280 x 0-720，导致点击位置偏移。

### 解决方案

**后端**：强制 `max_size=0`（禁用缩放），保证视频帧尺寸 == 设备物理分辨率

```python
# backend/app/infrastructure/stream/scrcpy.py
opts = EncoderOpts(
    max_size=0,       # 禁用缩放
    bit_rate=opts.bit_rate,
    codec=opts.codec,
    fps=opts.fps,
)
```

**前端**：收到 config 消息后立即设置 canvas 尺寸为设备分辨率

```typescript
// frontend/src/services/h264VideoStream.ts
if (width > 0 && height > 0) {
    this.canvas.width = width
    this.canvas.height = height
    this.canvas.style.aspectRatio = `${width} / ${height}`
}
```

### 经验

- **视频缩放会影响坐标映射**：scrcpy-server 接收的坐标是设备物理坐标，不是视频帧坐标
- `max_size=0` 表示不缩放，视频帧尺寸 == 设备物理分辨率
- 坐标映射的一致性：前端 canvas 尺寸 == 视频帧尺寸 == 设备物理分辨率
- 后端 config 消息必须携带设备分辨率（`width/height`），前端据此初始化 canvas

### 关联

- [E010 坐标映射必须基于视频实际尺寸](#e010-坐标映射必须基于视频实际尺寸而非设备物理分辨率)

---

## E014 视频流端到端调试方法论

**日期**：2026-09-11  
**标签**：`方法论` `调试` `视频流` `端到端`

### 症状

视频流无法显示，涉及前端（WebCodecs）、后端（WebSocket/scrcpy）、设备（编码器）多层，难以定位问题所在层。

### 调试流程

1. **分层诊断**：前端 → 代理 → 后端 → TCP → 设备，逐层排除
2. **日志关键点**：
   - 前端：`[H264] Config received` / `[H264] Decoder state` / `[H264] Decode failed`
   - 后端：`sps_received` / `config_sent` / `video_frame_sent`
   - 设备：`adb logcat | grep -E 'OMX|MediaCodec|scrcpy'`
3. **数据验证**：
   - 检查原始字节（hex dump）确认 SPS/PPS 结构
   - 验证 AVCC description 大小是否符合公式
   - 确认 codec string 格式（`avc1.XXYYZZ`）

### 常见问题链

```
SPS 索引错误
    ↓
profile/level 提取错误
    ↓
codec string 错误
    ↓
VideoDecoder configure 失败
    ↓
所有帧被丢弃
    ↓
15 秒超时 → 回退截图模式
```

或：

```
消息处理器注册晚于 connect()
    ↓
config 消息到达时 onMessage 为 null
    ↓
config 被丢弃
    ↓
VideoDecoder 未初始化
    ↓
所有帧被丢弃
```

### 经验

- **分层日志**：每层都有关键日志，快速定位问题所在层
- **数据流验证**：从源头（设备）到终点（浏览器）逐段验证
- **超时回退**：重要路径要有超时检测，避免无限等待
- **不要假设**：每层的假设都要用日志验证（如"config 应该收到了" → 查日志确认）

### 关联

- [E007 协议实现必须对照源码](#e007-协议实现必须对照源码)
- [E009 Rockchip 硬件编码器崩溃](#e009-rockchip-硬件编码器崩溃导致视频流中断)

---

## E015 scrcpy-server v4.1 升级注意事项

**日期**：2026-09-11  
**标签**：`scrcpy` `版本升级` `协议兼容性`

### 背景

项目从 scrcpy-server v2.4 升级到 v4.1（JAR 大小 733706 bytes）。

### 兼容性确认

- **协议握手**：v4.1 与 v2.4 握手格式相同（dummy byte + device name + resolution）
- **视频流格式**：`raw_stream=true` 输出原始 H.264 Annex B 流，无变化
- **控制协议**：触摸/按键/文本消息格式完全兼容
- **启动参数**：新增 `raw_stream=true` 禁用协议握手字段（dummy/device meta/codec header）

### 升级步骤

1. 替换 `backend/app/scrcpy/scrcpy-server.jar` 为 v4.1 版本
2. 更新 `constants.py`：`SCRCPY_SERVER_VERSION = "4.1"`
3. 验证启动参数兼容性（大部分参数通用）
4. 端到端测试：视频流 + 输入控制

### 注意事项

- `raw_stream=true` 跳过了协议握手读取分辨率的步骤，需要通过 ADB 获取设备分辨率
- JAR 文件每次 server 退出会被删除，必须每次启动前推送
- 不同版本的 scrcpy-server 可能有不同的参数支持，升级后需要测试

### 经验

- 升级第三方组件时，先确认协议兼容性
- JAR/二进制文件版本管理：文件名或注释标明版本
- 升级后必须端到端测试，不能只测试单个模块

### 参考

- scrcpy v4.1 源码：https://github.com/Genymobile/scrcpy
- JAR 文件：`backend/app/scrcpy/scrcpy-server.jar`（v4.1，733706 bytes）

---

## E016 /proc/net/tcp 地址格式解析

**日期**：2026-09-11  
**标签**：`网络` `Android` `proc`

### 背景

实现网络监控功能时，需要解析 `/proc/net/tcp` 文件获取 TCP 连接信息。

### 症状

直接读取 `/proc/net/tcp` 中的地址字段（如 `0100007F:0050`），无法直接使用。

### 根因

`/proc/net/tcp` 中的 IP 地址和端口号以十六进制编码，且 IP 地址是**小端序**（little-endian）。

格式：`hex_ip:hex_port`
- `0100007F` → 小端序解析为 `127.0.0.1`（不是 `7F000001`）
- `0050` → 十六进制端口 80

### 解决方案

```python
import struct

def parse_address(addr: str) -> tuple[str, int]:
    """解析 /proc/net 中的地址格式。"""
    parts = addr.split(":")
    ip_hex = parts[0]
    port_hex = parts[1]

    # 解析端口（大端序）
    port = int(port_hex, 16)

    # 解析 IP（小端序）
    ip_int = int(ip_hex, 16)
    ip_bytes = struct.pack("<I", ip_int)  # 注意 <I 是小端序
    ip_addr = f"{ip_bytes[0]}.{ip_bytes[1]}.{ip_bytes[2]}.{ip_bytes[3]}"

    return ip_addr, port
```

### 经验

- Linux proc 文件系统中的网络地址使用小端序编码
- 解析时需要用 `struct.pack("<I", ...)` 处理字节序
- `/proc/net/tcp6` 格式不同，IPv6 地址解析更复杂

### 参考

- Linux kernel 文档：`Documentation/networking/proc_net_tcp.txt`

---

## E017 dumpsys gfxinfo 获取 FPS 的正确方式

**日期**：2026-09-11  
**标签**：`性能监控` `Android` `FPS`

### 背景

实现性能监控时，需要获取设备实时帧率（FPS）。

### 症状

使用 `dumpsys gfxinfo <package>` 获取帧数，返回值是累计帧数（如 46887），不是实时 FPS。

### 根因

`dumpsys gfxinfo` 输出的是应用**启动以来的累计统计**：
- `Total frames rendered: 46887` — 累计渲染帧数
- `Janky frames: 2995` — 累计卡顿帧数

### 解决方案

**两次采样差值法**：

```python
class PerformanceSampler:
    def __init__(self):
        self._prev_frames: int | None = None
        self._prev_ts: float | None = None

    async def _get_fps(self, now: float) -> float | None:
        # 获取累计帧数
        total_frames = await self._get_total_frames()

        # 首次采样无法计算 FPS
        if self._prev_frames is None:
            self._prev_frames = total_frames
            self._prev_ts = now
            return None

        # 计算实时 FPS
        elapsed = now - self._prev_ts
        frame_delta = total_frames - self._prev_frames
        fps = frame_delta / elapsed if elapsed > 0 else 0

        self._prev_frames = total_frames
        self._prev_ts = now
        return fps
```

### 经验

- Android 没有直接提供"当前 FPS"的系统 API
- 实时 FPS 需要通过两次采样累计帧数的差值计算
- 首次采样返回 `null`（无前序数据），前端应显示 `-`
- 采样间隔建议 1-2 秒，间隔太短会导致 ADB 命令过于频繁

### 参考

- Android gfxinfo 文档：https://developer.android.com/topic/performance/graphics/debug-gfx
- `dumpsys gfxinfo` 命令参考

---

## E018 scrcpy 协议不支持运行中改码率，自适应只能重启编码器

**日期**：2026-09-17  
**标签**：`scrcpy` `协议` `自适应码率` `v4.1`

### 症状
实现「视频流自适应码率控制」时，希望根据客户端实测 fps 动态调高/调低 bit_rate。直觉做法是向 scrcpy-server 控制 socket 发一条"改码率"消息，但协议里找不到这样的消息。

### 根因
对照官方 Java 源码（scrcpy v4.1 与 v2.4 均如此）确认：
- **上游控制协议没有 CHANGE_STREAM_PARAMETERS / SET_BIT_RATE 这类运行中改码率消息。** ControlMessageTypes 类型 0–22 中，与"视频"相关的 `RESET_VIDEO=17` 仅触发重出一个 IDR 关键帧，不能改 bit_size/bit_rate。
- bit_rate 只在 server **启动参数**（`video_bit_rate`）里设定一次，进程生命周期内不可变。
- 注意区分：ws-scrcpy 仓库里能看到的"改流参数"是 **其自制 patch 版 server** 的私有能力，非上游 scrcpy，不能直接照搬。

结论：scrcpy 协议层面无运行中改码率通路，唯一可行路径是**按新码率档位重启 scrcpy-server 进程**。

### 解决方案
用「档位阶梯 + 事件驱动重启」实现，切换代价（每次重启约 1–4s 黑屏）靠决策器迟滞 + 冷却控制频率：

1. **决策器 `BitrateAdvisor`**（纯函数，易测）：客户端每 2s 上报实测 fps，维护滑动窗口（默认 8）。坏样本 ≥6 且 <0.7×target → 降一档；窗口全好且距上次切换 ≥ min_interval(30s)+up_extra(90s) → 升一档，上限=起始档。切换后清空窗口（防抖）。
2. **重启唤醒用事件与取帧协程赛跑**，不能"等下一帧时消费 pending 码率"：静止画面下 scrcpy 不出帧，帧循环会阻塞在 `agen.__anext__()`，pending 永远等不到。改为 `asyncio.wait({frame_task, restart_event}, FIRST_COMPLETED)`，事件先到也立即重启。（曾因此 bug：静止画面切档延迟 14s，修后 77ms。）
3. **重启间隙通知前端进宽限期**：WS 层下发 `{type:'restarting'}`，前端暂停 #4 回退 watchdog 与 stats 上报 15s，避免把"重启黑屏"误判为编码器崩溃、或把间隙内 fps≈0 污染决策窗口。
4. **epoch 递增触发解析器重置**：每次重启 `epoch+=1`，WS 层检测到变化即重建 `H264Parser` 并强制重发 config，避免跨编码器实例的陈旧 SPS/PPS。
5. **静止画面防误判**：客户端仅实测 fps≥1 才上报——有流但偏慢=疑似拥塞(交自适应)；fps<1≈无新帧=画面静止(交 #4 的 stall 判据，真崩溃才回退截图)。
6. **E2E 保确定性**：`config/e2e.yaml` 设 `stream.adaptive_bitrate: false`，playwright webServer 注入 `APP_ENV=e2e`，避免真机低帧画面随机触发重启黑屏使基线抖动。自适应逻辑本身由后端单测覆盖，不依赖 E2E。

### 经验
- 涉及协议的"能不能动态改 X"这类问题，**先对照上游源码确认消息类型集合，再动手**（见 E007），不要凭其他 fork 项目的表面能力臆断。fork（如 ws-scrcpy）的"新功能"很可能是 patch 出来的私有协议，与标准 server 二进制不兼容。
- 当协议只能"重启"时，工程重心就从"发指令"转到**控制重启频率（迟滞+冷却）+ 掩盖重启代价（宽限期+强制 IDR/config 重发）+ 唤醒时机不能被数据流阻塞（事件赛跑而非等下一帧）**。
- async 生成器要**首次 `__anext__()` 才执行函数体**：测试里等"新编码器已创建"，断言条件必须含 `opts is not None`，否则拿到半成品。

### 参考/关联
- scrcpy `ControlMessageTypes.java` / `Options.java`（bit_rate 仅启动参数）
- `app/application/bitrate_advisor.py`、`app/application/stream_service.py`、`app/interfaces/ws/video.py`
- `frontend/src/components/stream/VideoPlayer.vue`、`frontend/src/services/h264VideoStream.ts`
- 关联：[E007 协议实现必须对照源码](#e007-协议实现必须对照源码)、[E009 Rockchip 硬件编码器崩溃](#e009-rockchip-硬件编码器崩溃导致视频流中断)、[E015 scrcpy-server v4.1 升级](#e015-scrcpy-server-v41-升级注意事项)

---

## E019 TCP 可达性预检取代 adb connect 超时等待

**日期**：2026-09-18  
**标签**：`adb` `TCP` `连接优化` `超时`

### 症状
在设备连接界面输入不可达的 IP 后点「连接」，要等 21~30s 才弹出失败提示（断开不可达设备同理）。期间 UI 一直处于 loading，体验极差。

### 根因
`adb connect` 底层对不可达主机要等操作系统层面的 TCP SYN 重传阶梯耗尽才返回：本机实测
`adb connect 192.168.8.99:5555` 耗时 **21.2s** 才报 `10060（连接尝试失败）`，再叠加项目
命令级超时（`adb.timeout: 30`）兜底。失败耗时完全由内核重传策略决定，adb 与项目都无能为力。

### 解决方案
「连接/断开」前置一层纯 TCP 可达性预检（`asyncio.open_connection` + `wait_for`，默认
1.5s），不可达直接快速失败，不进入 adb 子进程（方案 `方案/16-设备连接可达性预检优化.md`）：

- `connect`：预检失败抛 `DeviceUnreachableError`（中文原因，HTTP 400 `DEVICE_UNREACHABLE`）。
- `disconnect`：预检失败跳过 adb 调用直接返回（清理无收益），应用层继续 repo.delete → 200。
- 实测：不可达连接 **21.2s → 1.55s** 返回 400；不可达断开 1.51s 返回 200。

### 经验
- **TCP 层快速失败必须自己做**：`adb connect` 没有可配置的短超时；对不可达主机等 SYN
  重传是内核行为，任何子进程级 timeout 都只是兜底。
- **本机 Windows 对回环关闭端口的 RST 有约 2s 延迟**：对 `127.0.0.1` 已关闭端口 connect
  需约 2.04s 才返回 `WinError 10061`（而非「立即拒绝」）。这会影响预检索因分类——默认
  1.5s 超时下「端口关闭」会归类为「超时」而非「拒绝」（两者都是不可达提示，功能无碍）；
  写测试时「拒绝」用例的 timeout 需放宽（>=5s）才能测到该分支。
- 预检异常 `DeviceUnreachableError` **不继承** `AdbError`：后者语义是「adb 子进程非零
  退出码」，预检失败发生在子进程启动之前，且现有 `except AdbError` 路径与预检不相交。
- 跳过 `adb disconnect` 会放大一个既有缺陷：adb 察觉掉线前表项仍是 `device` 状态，
  此时 `list_devices` 对该设备 `get_device_info` 失败会让整个列表 400 → 一并加固为
  单设备失败跳过 + 告警。

### 参考/关联
- `方案/16-设备连接可达性预检优化.md`
- `backend/app/infrastructure/adb/reachability.py`、`backend/app/infrastructure/adb/cli.py`（connect_tcp/disconnect_tcp）
- `tests/infrastructure/test_reachability.py`、`tests/infrastructure/test_adb_cli.py::TestTcpConnection`
- 关联：[E004 Git Bash 路径自动转换](#e004-git-bash-路径自动转换)

---

## E020 编码器静止出帧与 IDR 周期实测（RK3566）

**日期**：2026-09-18
**标签**：`scrcpy` `编码器` `Rockchip` `实测` `自适应码率`

### 现象（真机实测，192.168.8.33，方案 17 验证）

对 `tests/manual/capture_ws_frames.py` 抓取的 WS 帧流按起始码切 AU 离线解析：

1. **静止/空闲仍持续出帧**：屏幕无操作、关屏状态下仍 ~8-10fps 出帧
   （逐秒帧数 min=4 p50=10，零帧秒=0）；但 89.8% 的帧 ≤100B
   （中位数 15B 的近空 P 帧），带宽开销极低。
2. **IDR 两段式周期**：流启动后前 60 帧内**每 5 帧一个 IDR**（共 12 个，
   帧序列 Ipppp×12，尺寸从 16.6K 递增到 30K 后稳定）；之后进入
   **~57s 一个 IDR** 的稀疏稳态（10 分钟窗口 22 个关键帧）。
3. **无重复重发**：IDR 内容 12/12 唯一（非重发上一帧）；全量去重后
   仅 6 种载荷重复出现（字节相同的近空帧），相邻重复载荷 0。

### 根因/解释

- scrcpy-server `SurfaceEncoder` 设 `repeat-previous-frame-delay-us=100ms`
  （官方源码）：编码器无输出时按 100ms 周期发重复帧通知。本仓后端
  `video.py` 对空载荷包静默丢弃（`packet_mode` 分支），前端不受影响；
  而设备端编码器在静止时实际仍在输出近空 P 帧（非纯 repeat）。
- 启动命令未传 `i_frame_interval`（scrcpy 上游默认 10s），但实测稳态
  IDR 周期 42-57s（两机一致），属设备端编码器行为差异，非本仓参数。

### 结论（对自适应码率的安全性）

- `VideoPlayer.vue` 注释「静止画面 fps≈0」在 RK3566 上不成立：静止时
  上报 ~10fps，`measured < 1` 的上报 gate 不会触发，也不会误触降档；
  真正 0fps（编码器停）时 gate 拦截——两种情形都安全（防御性过滤）。
- IDR 稀疏期（42-57s）长于重连启动期（前 6s 密集 IDR），画面恢复
  实际依赖「watchdog 回退 → 重连/探测重启流」，而非等待长周期 IDR；
  当前无 RESET_VIDEO 控制消息（scrcpy 2.4+ 协议有 TYPE_RESET_VIDEO），
  如需「不重连式快速恢复」可后续补充。

### 复核方法

```bash
# 落盘抓包
PYTHONIOENCODING=utf-8 python tests/manual/capture_ws_frames.py \
    192.168.8.33:5555 60 --save cap.h264
# 离线解析：按起始码切 AU → 统计 IDR 位置/尺寸、P 帧尺寸分布、MD5 去重
```

### 关联

- 方案 17 实施项 4（repeat-previous-frame 确认）
- 关联：[E009 Rockchip 硬件编码器崩溃](#e009-rockchip-硬件编码器崩溃导致视频流中断)、[E021 rk3568 间歇出帧故障](#e021-rk3568-间歇出帧故障与码率档位无关)

---

## E021 rk3568 间歇出帧故障与码率档位无关

**日期**：2026-09-18
**标签**：`scrcpy` `编码器` `Rockchip` `实测` `降档实验`

### 现象（192.168.8.18，rk3568_r）

- 10 分钟抓包：241 帧（0.40fps），仅 44/600 秒有帧；出帧呈一阵一阵的
  突发，**非永久崩溃**（设备持续间歇出帧，与 E009 的 RK3288 崩溃型不同）。
- 120s 窗口：65 帧（0.54fps），12 秒有帧；启动期同样出现
  「前 60 帧每 5 帧一个 IDR」（与 .33 完全一致），后续长间隔 26.9-56.9s。
- 全程起始码非法包=0、每帧单 NALU、config 仅 1 次、无 error 消息——
  1b 包头协议在其故障状态下帧边界依然稳定。

### 降档实验（方案 17 实施项 4，补 E009 的 rk3568 未验证缺口）

- 做法：`config/base.yaml` `stream.bit_rate` 4M→2M，重启后端，同窗口 120s 对照。
- 结果：**帧数 66 vs 65、有帧秒数 12 vs 12——完全一致**；仅单帧字节下降
  （IDR 43K→27K、总字节 881K→562K，-36%）。**故障与码率档位无关**。
- E009 记录 RK3288 降参（分辨率/帧率/码率）无效，本次在 rk3568 上
  独立复现「降档无效」结论（仅降码率）。

### prefer-software 对照（同批验证）

- `.18` 流经前端 Playwright 实测：H264 解码 21 帧全部正常，
  **无花屏、无解码错误**；`?swdecode=1` 参数真机透传生效
  （configure 日志含 `hardwareAcceleration: prefer-software`），软解路径
  同样正常解码。
- 无「规避硬解花屏」差异可观测（花屏未复现），开关按设计保留备用；
  透传行为由单测覆盖（`h264VideoStream.test.ts`）。

### 经验

- 低端 Rockchip 故障设备的行为契约：出帧冻结 ≥3s → watchdog `stalled`
  正确回退截图模式（含「只读预览·输入不可用」提示）；恢复探测
  （suspend 期间计数 + config 暂存）能自动重建 H264，链路闭环。
- 降档实验设计要点：同窗口时长对照（启动期出帧占比会稀释短窗口结论）。

### 关联

- 方案 17 实施项 4（Rockchip 降档实验 / 解码硬件加速降级实验）
- 关联：[E009 Rockchip 硬件编码器崩溃](#e009-rockchip-硬件编码器崩溃导致视频流中断)、[E020 静止出帧与 IDR 周期](#e020-编码器静止出帧与-idr-周期实测rk3566)

---

## E022 视频流每次连接完整启停 scrcpy-server

**日期**：2026-09-18
**标签**：`scrcpy` `视频流` `性能` `watchdog`

### 现象

每次前端 WS 连接都会触发完整流程（后端日志可循）：

```
pushing_scrcpy_server → starting_scrcpy_server_encoder →
starting_scrcpy_server →（WS 断开）→ stopping_video_stream →
stopping_scrcpy_encoder → video_stream_stopped
```

- JAR 每次重推（server 退出时删除 JAR，见 E002），push 耗时抖动大：
  正常 ~1s，负载时段实测 **10.1s**（11:56:22.87→11:56:32.99）。
- 首帧延迟随之抖动：4.3s（正常）→ 14.1s（push 慢时）。
- 设备端无实例残留（断开即退出，`ps` 确认）。

### 影响

- 前端 `no-stream` watchdog（8s 未出画面回退截图模式）在启动慢时
  会**先回退截图模式**，随后 suspend 探测收到 config/帧后自动 resume
  重建 H264——用户可见「先截图、后自动切回 H264」的闪变。
  属设计内韧性行为（非 bug），但启动慢时段观感不佳。
- 高频连接（验证脚本连续跑）会放大设备负载，push 耗时进一步上升，
  形成「越连越慢」的假象。

### 经验

- 连续真机实验之间留缓冲；观测到启动变慢时先查 push 耗时与设备负载，
  勿归因于代码回归。
- 可选优化（未实施，记录备查）：近期刚启动过的流可加短时复用/宽限；
  或前端 `no-stream` 阈值区分启动阶段（如首次连接放宽到 12-15s）。

### 关联

- 关联：[E002 scrcpy-server JAR 自动删除机制](#e002-scrcpy-server-jar-自动删除机制)、[E009 Rockchip 硬件编码器崩溃](#e009-rockchip-硬件编码器崩溃导致视频流中断)

---

## E023 logcat -T 时间下界：语义、精度与「从此刻起收」

**日期**：2026-09-21
**标签**：`logcat` `adb` `调试` `方案17`

### 现象

惰性日志采集（方案 17 实施项 5）每次「开始录制」重建 `adb logcat -v threadtime` 时，
设备端现存缓冲被整段重放：旧日志以新 seq 二次入库（DB 重复条目、导出/统计失真），
WS 也把旧日志重播给前端。

### 根因

不带时间界的 logcat 从缓冲区头部开始输出，重启采集 = 全量重收当前缓冲。
修复需时间下界（`-T '<time>'`，审查留档 `docs/reviews/2026-09-21-方案17-推流优化与日志按需.md` 建议 1）。

### 解决方案 / 真机结论（Android 11 / SDK 30，192.168.8.18）

- `logcat -T '<time>'` 受支持（`logcat -h`：打印指定时刻起的行，且不隐含 `-d` 仍持续输出新行）。
  时间格式：`MM-DD hh:mm:ss.mmm...`、`YYYY-MM-DD ...`、或 epoch `sssss.mmm...`。
- **精度到亚秒有效**：下界 `…34.083` 排除 `…34.067` 的行、包含 `…34.366` 的行。
- **`date "+…%S.000"` 的 `.000` 是字面值**，下界最多提前 1 秒 → 同秒内早于下界的行仍被回放。
  改用 toybox 支持的 `%N` 纳秒才零重放：`-T "$(date "+%m-%d %H:%M:%S.%N")"`。
- **`-T <纯数字>` 按「计数」解析**（会回放 N 行；`-T 1` 带回 1 行历史），不可用于零重放。
- 时刻在**设备端**用 `$(date …)` 生成（避免主机/设备时钟差），命令经 `adb shell`
  **单参数**传入（同 `shell_stream` 约定），避免远端 shell 把含空格的时刻拆成两个参数。
- 落地：`backend/app/infrastructure/adb/cli.py` `_LOGCAT_SINCE_NOW_CMD`。
  端到端验证：停录→重录后 DB 标记行不翻倍（99 行 / 96 distinct）。

### 经验

- 「从此刻起」类时间下界一律在设备端取时；主机取时会引入时钟差/时区错位。
- 落地下界前用标记行实测边界（同秒内早/晚各一条）与格式兼容性，勿只信文档字面。

### 关联

- [建议 1 处置记录](reviews/2026-09-21-方案17-推流优化与日志按需.md)

---

## 经验记录规范

新增经验条目时，按以下格式：

```markdown
## E### [简短标题]

**日期**：YYYY-MM-DD  
**标签**：`标签1` `标签2`

### 症状
[用户/程序观察到的现象]

### 根因
[底层原因]

### 解决方案
[具体的正确做法，带代码示例]

### 经验
[可复用的通用原则]

### 参考/关联
[相关文档、源码链接、其他经验条目]
```

编号规则：`E###` 按添加顺序递增。
