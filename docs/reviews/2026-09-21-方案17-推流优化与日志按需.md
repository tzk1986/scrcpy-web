# 审查留档：方案 17 推流优化与日志按需（faf90a3..6a6f42e）

> 审查方式：独立 AI 走查（2026-09-21）；审查者不携带实现过程上下文。

## 审查范围

- 提交区间：`faf90a3..6a6f42e`（master，已推送）。实测 **14 个提交**（任务简述的「12 个」与 `git log faf90a3..6a6f42e` 不符，以仓库为准）、**39 个文件**、**+3851 / −395 行**。
- 主题分组（提交 → 主题）：
  - 方案/文档：`8868d2d`、`e253b05`（方案 17 评审修订）、`919ce46`、`6a6f42e`（状态与真机结论回写）、`48a0ee8`（真机 WS 抓包脚本）
  - 实施项 1a：`001610f`（H264Parser 起始码扫描重写 bytes.find + 回看修正）
  - 实施项 1a 尾：`12e3a0f`（视频帧按 Access Unit 聚帧）
  - 实施项 1b：`5e07963`（scrcpy-server 12 字节包头协议转正）
  - 实施项 2：`7fd2804`（前端解码路径治理：单遍扫描 / 积压丢帧保 key / 日志收敛）
  - 实施项 3：`39ca680`（perf 采样治理：去重 / 并发 / 降频 / 失联停采 / 超时兜底）
  - 实施项 5：`b66a547`（logcat 惰性采集）+ `d9674d4`（日志页按需连接）
  - 实施项 4：`ee6f850`（video WS TCP_NODELAY）、`1c1857f`（Page Visibility 降载 + swdecode 软解开关）
- 审查方法：全量 diff 逐文件走查；协议实现与 scrcpy 官方源码**逐字节对照**（由 `backend/app/scrcpy/scrcpy-server.jar` 内 `META-INF/version-control-info.textproto` 锁定 revision `fa57d7c6b47139797a8e103f87719740718887d4`，对照官方 `server/src/main/java/com/genymobile/scrcpy/device/Streamer.java`、`app/src/demuxer.c`）；前后端 WS 契约核对；方案文档宣称与实现比对；测试目录与四层架构约束核对。

## 发现清单

### 阻塞

无。

协议核心实现经逐字节对照官方源码确认无误：12B 包头（bit63=session、bit62=config、bit61=key、`PTS_MASK=(1<<61)-1`、4B 大端 size）、session 包位域（byte3 LSB=client_resized、offset 4/8 大端宽高）、握手顺序（1B dummy → 64B 设备名 → 4B codec id，`"h264"=0x68323634`、0=禁用、1=配置错误）、`raw_stream=true` 兜底关 meta 开关等均一致。半包/EOF（`IncompleteReadError` 静默结束）、媒体包超时丢整包、config 包阻塞不丢、epoch 重置、任务与监听器清理等边界核查未发现缺陷。

### 建议

- [backend/app/application/debug_service.py:252-269 + backend/app/infrastructure/adb/cli.py:352-361] 惰性采集在每次「开始录制」重建采集任务时重启 `adb logcat -v threadtime`，未带 `-T` 时刻界 → 设备端现存缓冲区整段重放，已采集过的日志以新 seq 重复入库（`backend/app/infrastructure/persistence/sqlite.py:236` 纯 INSERT 无唯一约束/去重），WS 恢复推送后前端可见旧日志重播 — 影响：DB 重复条目随「暂停→开始」次数累积（导出/统计失真），且与方案/17 第 471 行宣称的「开启时才从当前时刻起收」不符（功能可用、无数据丢失，可用「清理」兜底）— 建议处置：重启采集时绑定当前时刻（如 `-T "$(date '+%m-%d %H:%M:%S.000')"`）或在入库层对 (ts,pid,tid,tag,message) 幂等去重，二者取一，并把恢复语义补进暂停横幅文案。

### 观察

- [frontend/src/components/stream/VideoPlayer.vue:376、286-294] 挂载时若 tab 已处于隐藏态（后台标签打开设备页），仍无条件启动 fps 上报且输入默认启用，要等首次 `visibilitychange` 才纠正 — 影响：该边缘场景下降载策略在首个事件前不生效（`startH264StatsReporting` 内部先 `stopH264Stats`，无双定时器风险）— 建议：`onMounted` 按 `document.hidden` 初始化一次（低优先，触发条件罕见）。
- [frontend/src/components/debug/PerfView.vue:52-53] 「卡顿: N 帧/秒」标签与降频后口径不符：gfxinfo 每 5 轮采一次（`backend/app/infrastructure/performance/sampler.py:132-135`、288-291），非 gfxinfo 轮 jank 恒 0，gfxinfo 轮的 delta 实际覆盖约 5 秒窗口 — 影响：卡顿数值量级被误读（约 5×）— 建议：文案改「帧/采样窗口」或按窗口秒数归一。
- [backend/app/infrastructure/performance/sampler.py:127-131] `asyncio.gather` 未用 `return_exceptions`：单个子任务失败后其余不取消，最多继续跑满 `SHELL_TIMEOUT=8s` — 已核查影响有限：Python 3.8+ 的 gather 内部回调会回收迟到异常（不产生 "Task exception was never retrieved" 噪声），后续轮次正常发起 — 无需处置，记录备查。
- [方案/17-推流流畅度优化.md:246-247] 1b 做法第 2 步把「4B w + 4B h」误并入握手序列（实际为 codec id 后首包即 12 字节 session 包头，宽高在其中 offset 4/8）— 影响：方案读者可能误解字节流（实现正确）— 建议：后续修订补「12 字节包头」字样。
- [方案/17-推流流畅度优化.md:296-297 vs frontend/src/services/h264NalUtils.test.ts] 方案承诺的「mock 断言 extractNalus 调用次数为 1」未逐字落地：单遍扫描由纯函数测试 + 50 万字节 O(n) 回归护栏覆盖（该测试文件 83-93 行），但无「每帧仅调用一次」的计数断言 — 影响：未来若重引入二次扫描不会被测试拦截 — 建议：可在 `h264VideoStream.test.ts` 补一条调用计数断言（低优先）。
- 本次新增 18 个文件缺少文件末尾换行（`backend/app/scrcpy/stream_protocol.py`、`au_aggregator.py`、`backend/app/interfaces/ws/socket_opts.py`、`frontend/src/services/h264NalUtils.ts`、`frontend/vitest.config.ts` 及全部新测试文件等），与仓库既有文件约定不一致 — 影响：仅风格一致性（ruff 默认规则集不含 W292，CI 不受影响）— 建议：后续格式化时顺带补齐。
- [backend/app/interfaces/ws/socket_opts.py:23-27] TCP_NODELAY 走 `WebSocket._receive.__self__.transport` 私有路径，依赖 uvicorn/starlette 调用约定（已按锁定版本核验路径真实：uvicorn 0.52.1 / starlette 1.3.1），失败静默返回 False — 影响：上游升级后可能静默失效（无告警）— 建议：升级这两个依赖时将其列入回归清单（可复用 `tests/manual/capture_ws_frames.py` 观察）。
- 已核查无问题项（备查）：`_data_queue` maxsize=100，媒体包超时丢整包（包边界对齐无半帧损坏）、config 包「宁可阻塞也不丢」；`aggregate_aus` 悬空前缀跨 chunk 续接、AUD 显式分界、epoch 重置清 pending/config；`settings()` 为缓存单例，编码器与 WS 层 packet 模式判定一致；前端删 JSON 二进制探测成立（binaryType=arraybuffer 下 config 走 text 通道，契约未变）；Page Visibility / 回切 / 恢复的定时器启停与 `inputController.setEnabled` 均有测试覆盖；gfxinfo 因依赖 package 无法并入 gather（实现为 3 并发 + 1 串行，比方案字面「4 个采集 gather」更准）；新增 Python 测试全部位于 `tests/`（`tests/manual/capture_ws_frames.py` 为手动脚本，不匹配 pytest 收集规则，CI 不受影响）；严格四层结构未见跨层调用（新 `app/scrcpy/stream_protocol.py`、`au_aggregator.py` 与既有 `h264_parser.py` 同级放置）。
- 区间内提交数 14（任务简述 12），主题分组无遗漏；方案 17 真机结论数字与 `docs/经验记录.md` E020/E021/E022 口径一致（.33：6236 帧/10min≈10.39fps；.18：241 帧≈0.40fps）。

## 结论

**有条件通过**：协议实现与前后端契约核心无阻塞问题，测试与架构约束满足；条件为「建议」清单第 1 条（logcat 恢复录制整段重放与重复入库，与方案宣称不符）修复后可视为落结。

## 处置结果

待处置（存量补审）。建议优先级：① 建议第 1 条（logcat `-T` 绑定/去重）并入实施项 5 的收尾项处理；② 观察中「挂载即隐藏的边缘态」「卡顿标签口径」可随手修；③ 其余观察（换行、方案措辞、调用计数断言、gather 记录）低优先，可并入文档整理批次。