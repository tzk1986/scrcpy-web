# 审查留档：质量指标补全批次（6a6f42e..HEAD）

> 审查方式：独立 AI 走查（2026-09-21）；审查者不携带实现过程上下文。
> 所有数字均来自审查者在本地实际执行的命令输出，不采信提交信息宣称（宣称与实测的差异单独列出）。

## 审查范围

- 提交区间：`6a6f42e..HEAD`（master，未推送）。实测 **25 个提交**、**85 个文件**、**+13254 / −910 行**，与任务简述一致。
- 任务简述的主题分组未列出区间内另有的 2 个提交：`2b9cb88`（frontend 依赖 peer 冲突修复，`128f4e1` 据其删除 CI 的 `--legacy-peer-deps`）、`ac2614a`（进度追踪 #12 状态同步），二者已一并核对，无异常。
- 主题分组（提交 → 主题）：
  - 测试基建：`6a7b4fb`（aiosqlite 连接泄漏致 pytest 挂起的修复 + sessionfinish 兜底）
  - 后端覆盖率补全：`7b1f40e`（删 examples.py 死代码）、`2ab7326`（计划落档 + gitignore 覆盖率产物）、`376b44c`（video.py/app_service）、`4a484e6`（debug_service）、`9156d48`（scrcpy.py）、`742399a`（http 路由 6 文件 + http_testkit）、`8316d05`（sqlite）、`cae4c64`（network sampler）
  - 前端覆盖率补全：`40cd108`（services）、`898551e`（stores）、`86fda32`（关键组件 + 展示组件冒烟）
  - 后端 bug 修复：`d4ff154`（app_service 三 bug）、`8316d05`（PRAGMA 游标残留）、`cae4c64`（IPv6 字节序）、`742399a`（devices 路由遮蔽）、`96c8644`（logcat off-by-one）、`b845901`（logcat -T 绑定设备端时刻）
  - 前端 bug 修复：`ce190b6`（CommandPalette 非响应式）、`42bd2fa`（挂载即隐藏降载盲区 + 卡顿口径文案）
  - 门禁：`411df20`（vitest 分层阈值）、`128f4e1`（pytest-cov pin + CI 接入）
  - 审查机制固化：`2f51fc0`（DEVELOPMENT.md 变更审查章节 + 方案 17 补审留档）
  - 文档批次：`9830b6c`（归档 11 文档）、`71d318e`（7 处端口/CORS/版本一致性修复）、`c5815e2`（API/架构/用户手册三件套 + README/部署指南）

## 审查方法

1. **宣称复核（全部实际执行）**，实测结果汇总：

   | 项目 | 命令 | 实测结果 | 与宣称比对 |
   |------|------|---------|-----------|
   | 后端测试 + 覆盖率门禁 | `PYTHONPATH=backend python -m pytest tests/ --ignore=tests/integration --ignore=tests/e2e -q --cov=app --cov-fail-under=80` | **494 passed + 1 skipped，总覆盖率 84.32%**，门禁通过（EXIT=0），1 warning（fastapi testclient 的 StarletteDeprecationWarning，环境级与本批次无关） | 测试数与基线宣称一致；覆盖率见「观察 1」 |
   | 前端测试 + 分层门禁 | `cd frontend && npx vitest run --coverage` | **294 passed（23 文件），总覆盖 95.93% 语句/行**；services 99.73%、stores 100%、VideoPlayer 100%、LogcatView 100%、DebugPanel 100%（门禁线 80/80/70）全部达标，EXIT=0 | 与宣称基线（294 passed、约 95%）一致 |
   | ruff | `python -m ruff check backend/app` | All checks passed（EXIT=0） | 一致 |
   | mypy | `python -m mypy backend/app` | 59 文件 0 错（EXIT=0） | 一致 |
   | eslint | `npm run lint` | EXIT=0 | 一致 |
   | build | `npm run build` | 构建成功（EXIT=0，10.3s） | 一致 |

   各提交宣称的模块级数字逐一复核（pytest 覆盖率表 + `--collect-only` 实数）：video.py 100%、app_service 100%、debug_service 100%、sqlite.py 100%、network/sampler.py 100%、scrcpy.py 92%、http 六路由文件各 100% —— 全部吻合；用例数宣称 scrcpy_encoder 46 例 / network_sampler 32 例 / debug_service_extra 36 例 / sqlite_repo 20 例 / app_service 49 例 / http 路由 71 例，`--collect-only` 实收 46/32/36/20/49/71，**全部吻合**（部分文件用例数含 parametrize 展开，与 def 数不同属正常）。
2. **门禁有效性负向验证**（防「门禁写了但不拦」）：
   - vitest（2.1.9）glob 阈值实测可拦：`npx vitest run --coverage --coverage.thresholds."src/components/ui/**".statements=99.9` → EXIT=1，输出 `ERROR: Coverage for statements (94.54%) does not meet "src/components/ui/**" threshold (99.9%)`；全局阈值同理可拦。
   - pytest 门禁实测输出 `Required test coverage of 80% reached. Total coverage: 84.32%`，机制生效。
   - CI（`.github/workflows/ci.yml`）命令与本地一致（ubuntu 下 `PYTHONPATH=backend:.` 冒号分隔有效；`--cov=app` 经 PYTHONPATH 解析到 `backend/app`，本地实测已验证该解析路径）；`pytest-cov==7.1.0` 已 pin 进 dev 依赖；前端 `npx vitest run --coverage` 触发 thresholds；`npm ci` 去 flag 依赖 `2b9cb88` 的 lock 同步（package-lock.json 已随批次提交）。
3. **测试有效性抽查**（10 个新增文件）：`tests/unit/test_video.py`（618 行）、`tests/unit/test_http_devices.py`、`tests/infrastructure/test_network_sampler.py`、`tests/infrastructure/test_sqlite_repo.py`、`tests/scrcpy/test_scrcpy_encoder.py`、`tests/application/test_debug_service_extra.py`、`frontend/src/services/{api,websocket,videoStream}.test.ts`、`frontend/src/stores/device.test.ts`、`frontend/src/components/{debug/NetworkView,debug/PerfView,debug/AppsView,ui/DockPanel,ui/CommandPalette,stream/VideoPlayer,router/index}.test.ts`。重点核查「为凑覆盖率的空转测试」特征（过度 mock、只断言 mock 被调用、恒真断言、复制粘贴用例）。
4. **bug 修复走查**：8 个修复提交逐个 `git show` 读 diff，判断对症性、回归风险、同类遗漏（含 app_service 正则改动的其他调用点排查）。
5. **约束核查**：`git ls-files` 检查测试文件位置；区间 backend diff 的新增 import 行核查四层跨层调用。
6. **文档抽查**：docs/API.md 端点计数（文档 35 行 HTTP 表 + 3 个 WS 小节 vs 代码实测 34 个 `@router` HTTP 路由 + main.py `/health` = 35、WS 3 个，吻合）、抽 5 个端点逐字段核对、4 项「已修复」宣称逐条到代码验证；docs/架构.md 数据流 4.3 全链路常量核对（MAX_LOG_BUFFER=50000、LOGCOLLECT_STOP_GRACE=0.5、batch 100 条/0.1s、session_ttl_days=7、restore_max_sessions=20，均与代码一致）；docs/用户手册.md 抽 3 个功能核对 UI 真实存在（截图模式「只读预览 · 输入不可用」徽标 VideoPlayer.vue:59、1500ms 轮询 videoStream.ts:64、Ctrl+K 命令面板 CommandPalette.vue、导航键含多任务/菜单 VideoPlayer.vue:74-77）。
7. **副作用与断链核查**：`71d318e` 端口/CORS 改动对 vite.config.ts（8080/8765）、tests/e2e/playwright.config.ts（baseURL 8080、BACKEND_PORT 8765）、config/*.yaml（无 5173 残留）的三方一致性 grep；`9830b6c` 归档 11 文档的旧路径全仓 grep（排除 archive 自身）。

## 发现清单

### 阻塞

无。

8 个 bug 修复全部对症且未发现回归：
- `d4ff154`：`package:(.+)=([^=]+)$` 贪婪锚定最后一个 `=`，正确适配 Android 11+ 含 `==` 的 APK 路径且兼容老式无 `=` 路径；`_has_system_flag` 按空白分词要求独立 `SYSTEM` token，避免 `SYSTEM_FIXED|...` 复合标志误判，并在列表路径（block）与详情路径（全输出）共用同一函数；包不存在返回 None 与 `apps.py:97` 的 404 契约对齐。全仓 grep 确认无其他 `package:` 行解析调用点受正则改动影响。
- `8316d05`：`execute_fetchall` 消费 PRAGMA 游标，消除 VACUUM "SQL statements in progress"，修法标准。
- `cae4c64`：/proc/net/tcp6 按主机字节序逐 32-bit 字打印，统一逐字反转对小端 Android 设备正确（原纯 IPv6 分支直转导致 `::1` 解成 `::100:0`），回归用例 `test_parse_address_ipv6_pure_words_reversed` 锁定。
- `742399a`：`/batch/install`、`/events` 字面路由上移至 `/{device_id}` 之前，且补了「路由表顺序守卫」与 ASGI 层 SSE 不被遮蔽的测试，防复发。
- `96c8644`：threadtime `parts[2]/[3]/[4]` = PID/TID/LEVEL 映射修正正确（对照格式 `MM-DD HH:MM:SS.mmm PID TID LEVEL TAG: msg` 的 `split(None, 6)` 分词），原 xfail 用例转正为回归。
- `b845901`：`-T "$(date "+%m-%d %H:%M:%S.%N")"` 经 `adb shell` 单参数传入，在设备端取时刻防时钟差，`domain/ports.py` 契约与调用方文档同步，留档附真机零重放证据（方案 17 审查建议第 1 条的处置）。
- `ce190b6`：`commands` 由模块级普通变量改 `ref`，根因（`watch`/computed 对非响应式依赖只求值一次）判断正确，回归测试断言首次打开面板即渲染命令。
- `42bd2fa`：`onMounted` 按 `document.hidden` 补一次 `onVisibilityChange()`，该函数对 `h264Stream`/`inputController` 均有空值保护（VideoPlayer.vue:286-294），截图模式路径不受影响；卡顿文案改「帧/采样窗口」与后端 gfxinfo 每 5 轮降频的口径一致，且有测试锁定。

测试有效性抽查未发现空转测试：`test_video.py` 以真实 12B 包头协议字节 + TestClient 编排驱动，断言消息序列/字节内容/生命周期副作用；`test_sqlite_repo.py` 用真实临时 SQLite 不 mock；`test_scrcpy_encoder.py` 以离线 StreamReader 喂协议字节；组件测试断言渲染文本/DOM/emit 而非仅 mock 调用。`api.test.ts` 一族以 mock 调用参数为断言对象，但对纯转发薄层而言 URL 构造、`encodeURIComponent` 编码、默认端口 5555、40s 超时正是其全部行为，属合理口径，不判为凑数。未发现恒真断言与复制粘贴重复用例。

### 建议

1. [backend/app/interfaces/http/debug.py:52] `create_session` 端点 docstring 副作用仍写「启动后台 logcat 收集任务」，与实际惰性启动行为相反（`application/debug_service.py:83-84` 与 docs/API.md 2.5、docs/架构.md 4.3 均正确）；同类：[backend/app/application/device_service.py:17,24-25] 模块 docstring 称「batch_install 当前是顺序执行…考虑使用 asyncio.gather + 信号量」，而实现（:127-141）早已是信号量 5 并发，docs/API.md 的「并发执行，信号量并发上限 5」才是对的 — 影响：两处均为存量陈旧注释（本区间未触碰这两个文件），但文档批次宣称「核对源码」，源码内注释与文档互相矛盾会误导后续维护与审查 — 建议处置：随下个批次把两处注释改齐实际行为。
2. [DEVELOPMENT.md:544-560] 「关键逻辑注释」示例代码仍是 `96c8644` 修复前的错误映射：示例第 559 行 `level = parts[2]`，而同示例自己的字段注释写明 1234=PID、5678=TID、I=级别（`split(None,6)` 后应为 `level=parts[4]、pid=parts[2]、tid=parts[3]`），示例内部自相矛盾 — 影响：DEVELOPMENT.md 是开发者与 AI 必读文档，照抄示例会把刚修掉的 off-by-one 写回去 — 建议处置：同步示例代码为修复后的映射（或改为引用 `debug_service._parse_logcat_line` 现行实现）。
3. [backend/app/infrastructure/transport/websocket.py:15-16、webtransport.py] 两个 0% 覆盖的死模块：全仓无任何 import（仅注释提及），`websocket.py` 头注释宣称「在 interfaces/ws/video.py 中创建 WebSocketTransport 实例」与事实不符（video.py 直接操作 FastAPI WebSocket）— 影响：存量问题（最后触碰于区间前的 faff3a8），永久拉低总覆盖率约 23 语句，且注释误导 — 建议处置：删除两模块（`domain/ports.py` 的 Transport 协议可保留），或在真正接入时再补；不建议为凑覆盖而给死代码写测试。
4. [frontend/src/services/websocket.ts:16] 头注释示例 `ws://localhost:8000` 为陈旧端口，`71d318e`「7 处修正」批次遗漏；同类遗漏：[tests/integration/test_debug_ws.py:33,46,145] 三处 `localhost:8000`（集成测试需手工起后端，按项目默认 8765 起服务会全部连接失败）、[方案/实施指南.md:42] 与 [方案/10-集成测试与部署.md:475] 仍写「前端：http://localhost:5173」（后者与同文件第 15 行「8080 非 5173」自相矛盾）— 影响：文档/注释与运行配置不一致，集成测试按注释起服务必失败 — 建议处置：并入下一次文档一致性修正统一改为 8765/8080。

### 观察

1. **覆盖率数字漂移（宣称 vs 实测唯一不符处）**：`128f4e1` 提交信息称「本地实测 84.03%」，当前 HEAD 实测 **84.32%**——该提交之后 `b845901`/`42bd2fa` 等又新增了测试，数字上浮属正常演化，非宣称失实；门禁口径以 HEAD 实测为准。其余全部宣称值（后端 494+1、前端 294、各模块覆盖率与用例数）复核一致，见「审查方法」表。
2. [frontend/vitest.config.ts:24-30] 分层门禁仅约束 `src/services/**`、`src/stores/**` 与 3 个点名组件；views、其余组件、router 无覆盖率下限（注释标明「用户拍板口径：纯展示组件只做 mount 冒烟不卡线」）。新增 services/stores 文件会自动落入 glob，新增关键组件需手工点名。另 `coverage.exclude`（:21）为自定义列表，会整体覆盖 vitest 默认 exclude——当前已显式排回测试文件与 main.ts/App.vue，后续在 src 下新增入口类文件需记得维护该列表。
3. [tests/unit/http_testkit.py] 公共测试工具放在 `tests/unit/` 且文件名不匹配 pytest 收集规则（不会被误当用例），`test_http_devices.py` 以相对导入使用，`tests/unit/__init__.py` 存在，收集正常（71 例实测）。
4. 测试文件位置合规：Python 测试全部位于 `tests/`（`git ls-files` 核查 backend/ 与项目根无 `test_*.py`；`docs/superpowers/plans/*testing*.md` 为方案文档非测试）；前端测试与源码同目录放于 `frontend/src/`，为 vitest 社区约定且沿袭本仓既有惯例（区间前已有 `h264NalUtils.test.ts`），CLAUDE.md 的禁止范围（项目根、backend/）不涉及，判合规。
5. 四层架构：区间内 backend 改动无新增 import 行（diff 核查），未发现新的跨层调用；`app/scrcpy` 协议包地位未变。
6. `71d318e` 副作用复核：`frontend/vite.config.ts`（dev 8080 → 代理 8765）、`tests/e2e/playwright.config.ts`（baseURL http://localhost:8080、BACKEND_PORT 默认 8765）、`config/base.yaml`/`dev.yaml`/`settings.py`（CORS 均 8080，无 5173 残留）、`scripts/dev.sh`（改走 run_server.py，获得 ProactorEventLoop）三方端口口径一致，未发现依赖旧端口/旧 CORS 的活跃代码（integration 测试的 8000 为存量遗留，见建议 4）。
7. `9830b6c` 归档断链核查：对 11 个移动文档的旧路径全仓 grep（排除 docs/archive/ 自身），引用处（DEVELOPMENT.md:150,712,744、README.md、docs/经验记录.md:85）均已改指 `docs/archive/` 新路径，其余命中为叙述性文字非链接；`docs/archive/README.md` 索引存在。无断链。
8. `6a7b4fb` 修复核查：三个 fixture 由「仅从 `_POOLS` 摘除」改为 `await pool.close()`/`await repo.close()`，`tests/conftest.py:28-49` 增加 `pytest_sessionfinish` 兜底遍历关闭残留池——双保险合理；本地全量实测 27.5s 正常退出（EXIT=0），挂起不复现。
9. `7b1f40e` 删除 `backend/app/scrcpy/examples.py`（276 行）：全仓 grep 无代码引用（仅历史方案文档叙述性提及），pyproject mypy overrides 亦无 `app.scrcpy.examples` 残留条目，删除安全。
10. 本次复核实测环境为 Windows Git Bash + Python 3.10（CI 为 ubuntu + 3.11），494 passed 全绿说明批次未引入版本敏感代码；pytest 唯一 warning 来自 fastapi/starlette testclient 弃用提示，属环境级，与本批次无关。
11. 工作区在审查时存在 2 个未提交修改（`docs/superpowers/plans/2026-09-19-quality-metrics-completion.md`、`方案/进度追踪.md`），属批次收尾的进行中状态回写，审查未触碰；推送前需一并提交或还原。

## 结论

**通过**。

- 宣称复核：后端 494 passed + 1 skipped / 84.32%（门禁 80 通过），前端 294 passed / 95.93%（分层门禁全部达标），ruff / mypy / eslint / build 四项静态检查全绿；各提交宣称的模块覆盖率与用例数经实测逐一吻合，唯一数字差异（84.03 → 84.32）为后续提交继续补测的正常上浮。
- 门禁经负向验证确实可拦退化；测试抽查未见凑数空转；8 个 bug 修复对症且带回归测试；测试位置与四层架构约束合规；归档无断链；端口/CORS 一致性修复无副作用。
- 无阻塞项。4 条建议项均为注释/文档一致性或存量死代码问题，不影响行为正确性，可随后续批次处理。

## 处置结果（2026-09-21 回写）

审查结论「通过、0 阻塞」。4 条建议处置如下：

- **建议 2（DEVELOPMENT.md off-by-one 示例）——已修复**：示例改为 `level=parts[4]、pid=parts[2]、tid=parts[3]`，与 `debug_service._parse_logcat_line` 现行实现（:432-434）一致，消除「照抄示例把已修 bug 写回」的风险。
- **建议 1（两处源码陈旧注释）——已修复**：`interfaces/http/debug.py:52` 副作用注释改为「logcat 不随会话创建启动，订阅者开启录制时惰性启动」（对齐 `debug_service.py:83-84`）；`application/device_service.py:17,24-25` 模块 docstring 改为「batch_install 并发执行（信号量上限 5）」（对齐实现 :127-141）。
- **建议 4（端口一致性）——部分修复 + 部分转记遗留**：
  - 已修复：`frontend/src/services/websocket.ts:16` 注释 8000→8765；`方案/实施指南.md`（手动启动 `uvicorn ... --port 8000` → `python run_server.py`，兼得 ProactorEventLoop；访问 URL 5173/8000 → 8080/8765）；`方案/10-集成测试与部署.md:475-477`（5173/8000 → 8080/8765，消除与同文件第 15 行现实注记的自相矛盾）。
  - **转记遗留（不在本批次零散改）**：`tests/integration/` 整目录为陈旧 POC 脚本群——约 15 个文件硬编码**未授权设备 192.168.8.22:5555**、多处 `localhost:8000`、引用 scrcpy-server **v2.4**（项目现为 v4.1）与 `tools\adb.exe` 路径。该目录不入 CI、需手工起后端 + 真机，单改端口无法使其可运行。建议作为独立「集成测试目录归档/现代化」决策处理。
- **建议 3（transport 死模块）——暂缓（需用户取舍）**：`infrastructure/transport/websocket.py`、`webtransport.py` 确为 0% 覆盖、全仓无 import 的死模块。但 `webtransport.py` 是 README 明示的「WebTransport 保留占位（尚未启用）」，删除属需签字的方向性取舍；23 语句对全局覆盖率（现 88.78%）影响可忽略。记入遗留，待「真正接入 or 删除」决策。

**附带（非审查发现，独立处置）**：计划文档 B7 项（`adb/shell.py` ≥75%）此前被全局门禁掩盖为虚假完成（实测 0%，160 语句全 miss）。已补 `tests/infrastructure/test_shell.py`（39 例）将其补至 **99%**，后端全量 533 passed、全局覆盖率 **84.32%→88.78%**（提交 `12b85a0`）。该缺口属计划文档勾选项、非任何提交的宣称，故未进入本审查的「宣称复核」范围。
