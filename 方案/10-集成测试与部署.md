# 10 - 集成测试与部署

## 目标

完成端到端集成测试，Docker 部署，文档完善，发布 MVP。

## 状态

- ⏳ E2E 测试
- ⏳ Docker 配置
- ⏳ CI/CD
- ⏳ 文档完善
- ⏳ MVP 发布

## 依赖

- 所有前置模块

## 集成测试

### E2E 测试场景

**文件**：`backend/tests/e2e/test_full_flow.py`

```python
import pytest
from fastapi.testclient import TestClient
from app.main import app

@pytest.fixture
def client():
    return TestClient(app)

def test_device_list_and_connect(client: TestClient):
    """Test full device management flow"""
    # 1. 列出设备
    response = client.get("/api/devices")
    assert response.status_code == 200
    devices = response.json()
    
    if not devices:
        pytest.skip("No devices connected")
    
    device_id = devices[0]["id"]
    
    # 2. 获取设备详情
    response = client.get(f"/api/devices/{device_id}")
    assert response.status_code == 200
    device = response.json()
    assert device["id"] == device_id
    
    # 3. 创建调试会话
    response = client.post("/api/debug/sessions", params={
        "device_id": device_id,
        "user_id": "test-user",
    })
    assert response.status_code == 200
    session_id = response.json()["session_id"]
    
    # 4. 查询日志
    response = client.get(f"/api/debug/sessions/{session_id}/logs")
    assert response.status_code == 200
    logs = response.json()["logs"]
    assert isinstance(logs, list)
    
    # 5. 执行 Shell 命令
    response = client.post(f"/api/debug/sessions/{session_id}/shell", params={
        "command": "echo hello",
    })
    assert response.status_code == 200
    output = response.json()["output"]
    assert "hello" in output
    
    # 6. 关闭会话
    response = client.delete(f"/api/debug/sessions/{session_id}")
    assert response.status_code == 200

def test_video_stream(client: TestClient):
    """Test video streaming"""
    # 列出设备
    response = client.get("/api/devices")
    devices = response.json()
    
    if not devices:
        pytest.skip("No devices connected")
    
    device_id = devices[0]["id"]
    
    # WebSocket 视频流
    with client.websocket_connect(f"/ws/video/{device_id}") as websocket:
        # 接收几帧
        for _ in range(5):
            data = websocket.receive_bytes()
            assert len(data) > 0
        
        # 发送触摸事件
        websocket.send_json({
            "action": "touch",
            "x": 100,
            "y": 200,
        })
```

### 前端 E2E 测试

**文件**：`frontend/tests/e2e/device.spec.ts`

```typescript
import { test, expect } from '@playwright/test'

test.describe('Device Management', () => {
  test('should list devices', async ({ page }) => {
    await page.goto('/')
    
    // 等待设备列表加载
    await page.waitForSelector('.el-table')
    
    // 验证表格存在
    const table = await page.locator('.el-table')
    expect(table).toBeTruthy()
  })
  
  test('should connect to device', async ({ page }) => {
    await page.goto('/')
    
    // 等待设备列表
    await page.waitForSelector('.el-table')
    
    // 点击第一个设备的"连接"按钮
    const connectButton = await page.locator('button:has-text("连接")').first()
    if (await connectButton.isVisible()) {
      await connectButton.click()
      
      // 验证跳转到设备详情页
      await page.waitForURL(/\/device\/.*/)
      
      // 验证调试面板存在
      const debugPanel = await page.locator('.debug-panel')
      expect(debugPanel).toBeTruthy()
    }
  })
})

test.describe('Debug Panel', () => {
  test('should display logcat', async ({ page }) => {
    await page.goto('/')
    await page.waitForSelector('.el-table')
    
    const connectButton = await page.locator('button:has-text("连接")').first()
    if (await connectButton.isVisible()) {
      await connectButton.click()
      await page.waitForURL(/\/device\/.*/)
      
      // 切换到 Logcat 标签
      await page.click('text=Logcat')
      
      // 等待日志加载
      await page.waitForSelector('.log-list')
      
      // 验证日志条目存在
      const logs = await page.locator('.log-entry')
      expect(await logs.count()).toBeGreaterThan(0)
    }
  })
  
  test('should execute shell command', async ({ page }) => {
    await page.goto('/')
    await page.waitForSelector('.el-table')
    
    const connectButton = await page.locator('button:has-text("连接")').first()
    if (await connectButton.isVisible()) {
      await connectButton.click()
      await page.waitForURL(/\/device\/.*/)
      
      // 切换到 Shell 标签
      await page.click('text=Shell')
      
      // 等待终端加载
      await page.waitForSelector('.terminal')
      
      // 输入命令
      await page.keyboard.type('echo hello\n')
      
      // 验证输出
      await page.waitForSelector('text=hello')
    }
  })
})
```

## Docker 部署

### Backend Dockerfile

**文件**：`docker/Dockerfile.backend`

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    android-tools-adb \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 复制 Python 文件
COPY pyproject.toml .
COPY backend/ backend/
COPY config/ config/

# 安装依赖
RUN pip install --no-cache-dir -e .

# 创建数据目录
RUN mkdir -p /data

# 暴露端口
EXPOSE 8000

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

# 启动
CMD ["uvicorn", "backend.app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Frontend Dockerfile

**文件**：`docker/Dockerfile.frontend`

```dockerfile
FROM node:20-alpine as builder

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json* ./
RUN npm ci

COPY frontend/ .
RUN npm run build

# 生产镜像
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html

# Nginx 配置
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost/ || exit 1

CMD ["nginx", "-g", "daemon off;"]
```

### Nginx 配置

**文件**：`docker/nginx.conf`

```nginx
server {
    listen 80;
    server_name localhost;
    
    root /usr/share/nginx/html;
    index index.html;
    
    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 代理
    location /api {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
    
    # WebSocket 代理
    location /ws {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
}
```

### Docker Compose

**文件**：`docker-compose.yml`（已创建）

```yaml
version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=prod
      - DB_PATH=/data/debug.sqlite
    volumes:
      - ./data:/data
      - ./config:/app/config
    restart: unless-stopped
    depends_on:
      - adb

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

  adb:
    image: sorccu/adb:latest
    privileged: true
    volumes:
      - /dev/bus/usb:/dev/bus/usb
    restart: unless-stopped

volumes:
  data:
```

## CI/CD

### GitHub Actions

**文件**：`.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
      
      - name: Lint
        run: |
          ruff check backend/app
          mypy backend/app
      
      - name: Test
        run: |
          pytest backend/tests/unit
  
  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Lint
        run: |
          cd frontend
          npm run lint
      
      - name: Build
        run: |
          cd frontend
          npm run build
  
  docker:
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-test]
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v3
      
      - name: Build Docker images
        run: |
          docker compose build
      
      - name: Test Docker
        run: |
          docker compose up -d
          sleep 10
          curl -f http://localhost:8000/health
          docker compose down
```

## 文档完善

### README 更新

**文件**：`README.md`

```markdown
# OpenScrcpy

scrcpy Web 化开源项目 —— 基于 Python (FastAPI) + Vue 3 的多设备远程控制与调试平台。

## 特性

- ✅ 零安装：浏览器直接访问
- ✅ 无限调试：持久化 logcat、远程 Shell、性能监控
- ✅ 多设备管理：同时管理 100+ 台 Android 设备
- ✅ 低延迟视频流：WebCodecs + WebSocket
- ⏳ 团队协作：屏幕共享、多人控制（计划中）

## 快速开始

### 开发模式

```bash
make dev
```

访问：
- 前端：http://localhost:5173
- 后端：http://localhost:8000
- API 文档：http://localhost:8000/docs

### Docker 部署

```bash
docker compose up -d
```

访问：http://localhost

## 系统要求

- Python 3.11+
- Node.js 20+
- ADB (Android Debug Bridge)
- Chrome 浏览器（最新两个稳定版）

## 文档

- [架构设计](docs/architecture.md)
- [API 文档](docs/api.md)
- [调试功能](docs/debug-features.md)
- [实现方案](方案/README.md)

## 许可证

MIT License
```

## MVP 发布清单

### 功能检查

- [x] 设备管理（列出、详情、截图）
- [x] 视频流（WebSocket 传输）
- [x] 输入控制（触摸、滑动、按键）
- [x] Logcat 日志（实时、过滤、导出）
- [x] 远程 Shell（xterm.js）
- [ ] 性能监控（基础）
- [ ] 网络抓包（计划 Phase 2）

### 质量检查

- [ ] 单元测试覆盖率 > 80%
- [ ] E2E 测试通过
- [ ] 性能测试达标
- [ ] 安全审查完成

### 文档检查

- [x] README 完善
- [x] API 文档
- [x] 架构文档
- [x] 调试功能文档
- [ ] 部署指南
- [ ] 故障排查指南

### 发布步骤

1. 更新版本号（pyproject.toml, package.json）
2. 更新 CHANGELOG.md
3. 打 Git tag（v0.1.0）
4. 构建 Docker 镜像
5. 发布 GitHub Release
6. 更新文档网站

## 验收标准

- ⏳ 所有 E2E 测试通过
- ⏳ Docker 部署成功
- ⏳ CI/CD 流水线正常
- ⏳ 文档完整
- ⏳ MVP 发布

## 交付物

- ⏳ E2E 测试套件
- ⏳ Docker 配置文件
- ⏳ CI/CD 配置
- ⏳ 完整文档
- ⏳ v0.1.0 发布

## 下一步

1. 完成 E2E 测试
2. 完善 Docker 配置
3. 配置 CI/CD
4. 完善文档
5. 发布 MVP

## 参考

- [Docker 文档](https://docs.docker.com/)
- [GitHub Actions](https://docs.github.com/en/actions)
- [FastAPI 部署](https://fastapi.tiangolo.com/deployment/)
