# OpenScrcpy 项目进度追踪

## 总体进度

**Phase 1: MVP + 无限调试**（6-8 周）

| 周次 | 模块 | 状态 | 完成度 | 备注 |
|------|------|------|--------|------|
| Week 1-2 | 后端基础架构 | ✅ 完成 | 90% | 脚手架已搭建 |
| Week 1-2 | 设备管理服务 | ✅ 完成 | 98% | UI 完善：添加/断开设备 + SSE 实时监听 |
| Week 3-4 | 视频流服务 | ✅ 完成 | 98% | H.264 NAL 解析 + SPS/PPS 配置 + 并发输入处理 + 端到端验证 + scrcpy 二进制控制 + 坐标映射修复 |
| Week 3-4 | 前端视频播放器 | ✅ 完成 | 95% | WebCodecs VideoDecoder + Annex B→AVCC + 模式回退 + 前端构建通过 + H264 点击控制修复 |
| Week 5-6 | 调试会话管理 | ✅ 完成 | 95% | 连接池 + 批量写入 + 断线续传 + 会话恢复（余并发锁） |
| Week 5-6 | Logcat 日志系统 | ⏳ 进行中 | 95% | 智能精简 + 自动清理 + 导出 |
| Week 5-6 | 远程 Shell | ⏳ 进行中 | 90% | ✅ 流式输出 + 命令历史 + PTY 伪终端（ConPTY/pywinpty） |
| Week 5-6 | 性能监控 | ✅ 完成 | 95% | Perf 标签完成（CPU/MEM/FPS/Activity） |
| Week 7-8 | 前端调试面板 | ✅ 完成 | 95% | 5 个标签页全部实现（Logcat/Shell/Perf/Apps/Network） |
| Week 7-8 | 集成测试与部署 | ✅ 完成（地基版） | 90% | 测试治理(14→0)/ruff/mypy strict 清零/ESLint9/GitHub Actions CI/部署文档；exe 打包另跟进 |

## 模块详细状态

### ✅ 已完成

#### 后端基础架构（90%）
- ✅ 项目脚手架
- ✅ 配置管理（YAML + Pydantic）
- ✅ 依赖注入
- ✅ 结构化日志（structlog）
- ✅ 异常处理
- ✅ 生命周期管理
- ⏳ 配置热重载（低优先级）

#### 项目脚手架（100%）
- ✅ 目录结构
- ✅ 分层架构
- ✅ 核心抽象接口
- ✅ Docker 配置
- ✅ Makefile

#### 性能监控（95%）
- ✅ PerformanceSampler：CPU/MEM/FPS/Activity 采集（infrastructure/performance/sampler.py）
- ✅ NetworkSampler：流量/连接/WiFi 采集 + 采样器实例缓存（修复速率计算）
- ✅ PerformanceService：内存环形缓冲 + pub/sub
- ✅ HTTP `/api/perf`、`/api/network` + WS `/ws/perf/{device_id}`
- ✅ 前端 PerfView / NetworkView（ECharts 实时曲线 + 指标卡片）
- ✅ FPS 应用切换负值修复（2026-09-11）

### ⏳ 进行中

#### 设备管理服务（98%）
- ✅ 领域模型（DeviceInfo）
- ✅ ADB 驱动抽象
- ✅ ADB CLI 驱动完整实现（错误处理、连接检测、TCP/IP）
- ✅ TCP/IP 连接可达性预检（connect/disconnect 前置 1.5s TCP 探测，不可达快速失败，见 `方案/16`）
- ✅ DeviceService 完整实现（后台刷新、事件回调、批量优化）
- ✅ HTTP 端点（设备连接管理、SSE 事件流）
- ✅ **前端 Dashboard 设备管理 UI**
  - ✅ 添加设备对话框（输入 IP/端口，TCP/IP 连接）
  - ✅ 断开设备按钮（TCP/IP 设备显示断开按钮）
  - ✅ SSE 实时设备变化监听（自动更新列表）
  - ✅ 空状态提示（无设备时引导用户添加）
  - ✅ TCP/IP 设备识别（根据 device_id 格式判断）
- ✅ 单元测试（16 + 28 = 44 个测试全部通过）
- ⏳ 真实设备集成验证

#### 调试会话管理（95%）
- ✅ 领域模型（DebugSession）
- ✅ DebugService 完整实现（含 WebSocket 订阅）
- ✅ SQLite 持久化
- ✅ DebugService/DeviceService 单例（@lru_cache 修复状态共享）
- ✅ 连接池（DatabasePool，aiosqlite 复用 + WAL，DEBUG_DB_POOL_SIZE 可配）
- ✅ 批量写入（BatchLogWriter，100 条/100ms executemany 单事务，实测 >5000 条/秒）
- ✅ 断线续传（seq + subscribe from_seq/log_batch + 前端自动重连补发）
- ✅ 会话恢复（lifespan restore_sessions，TTL 内 + 上限 20）
- ⏳ 并发控制（exec_shell 会话锁，留后续）

#### Logcat 日志系统（95%）
- ✅ 后端 logcat 收集
- ✅ 日志解析
- ✅ SQLite 存储
- ✅ WebSocket 实时推送（5378 条/10s ≈ 538 条/秒）
- ✅ 前端实时日志显示（自动滚动 + LIVE 指示器）
- ✅ 前端虚拟滚动（vue-virtual-scroller，支持 50K 日志）
- ✅ 服务端过滤（按 level/tag，减少网络流量）
- ✅ 导出功能（JSON + CSV 格式）
- ✅ **智能日志精简**（按级别过滤 + 速率限制，大幅减少存储）
- ✅ **自动清理机制**（按时间/容量自动删除旧日志）

#### 远程 Shell（75%）
- ✅ ShellView 组件框架（xterm.js）
- ✅ 后端 exec_shell（HTTP）
- ✅ WebSocket 命令执行（通过 store 路由）
- ✅ 命令历史记录（上/下箭头浏览，最多 100 条）
- ✅ Ctrl+C 取消、Ctrl+L 清屏
- ✅ **流式输出**（shell_stream 消息逐行实时推送）
  - ✅ AdbDriver.shell_stream() 协议方法
  - ✅ AdbCliDriver.shell_stream() 实现（asyncio.subprocess + readline）
  - ✅ DebugService.exec_shell_stream() 应用层
  - ✅ WS handler 流式发送 shell_stream 消息
  - ✅ debug store execShellWs() 支持 streamCallback
  - ✅ ShellView 流式写入终端
  - ✅ 端到端测试通过（10 行流式输出 + shell_output 完成）
- ✅ **PTY 伪终端**（2026-09-17）
  - ✅ 设备侧 `adb shell -tt` 强制 PTY（InteractiveShell）
  - ✅ 宿主侧 ConPTY 接入（pywinpty 3.x，`infrastructure/adb/winpty.py`）
  - ✅ chcp 65001 包装 → 中文 UTF-8 全链路无损（默认 CP936 乱码已解）
  - ✅ taskkill /T 进程树清理；`adb.use_conpty` 开关，失败自动降级管道
  - ✅ .22 真机冒烟通过：prompt 检测 / 持久状态(cd) / 中文往返 / 原始按键回显
  - ✅ USE_CONPTY=false 降级管道路径验证通过；E2E shell fixture 在 ConPTY 后端下通过
    （.34 当前离线，E2E 换 .22 跑：8 passed；rotation 用例在 .22 不支持旋转故 failed，
    属设备差异非代码回归，原基线 .34 上该项为 skip；通知栏下拉用例亦为 .22 ROM 行为差异）

#### 前端调试面板（90%）
- ✅ DebugPanel 框架
- ✅ LogcatView 实时日志 + 虚拟滚动
- ✅ ShellView 命令终端 + 命令历史
- ✅ 可停靠面板（底部/右侧切换，DockPanel 组件）
- ✅ 拖拽调整大小
- ✅ 快捷键（Ctrl+` 切换位置、Ctrl+L 聚焦 Logcat）
- ✅ 命令面板（Ctrl+K）

### ⏳ 待开始

#### 视频流服务（98%）- 方案A
- ✅ 领域模型（VideoEncoder）
- ✅ ScrcpyEncoder 完整实现（scrcpy-server.jar 直接集成）
- ✅ 协议握手（dummy byte + 设备名 + codec + 分辨率）
- ✅ 双 socket 连接（视频 + 控制）
- ✅ asyncio.Queue 数据传输
- ✅ StreamService 应用层服务
- ✅ WebSocket 端点 /ws/video/{device_id}
- ✅ 多设备并发支持
- ✅ H.264 NAL 单元解析（H264Parser，Annex B 格式）
- ✅ SPS/PPS 配置提取与发送（config 消息）
- ✅ 视频帧（IDR/P/B）二进制转发
- ✅ 并发输入处理（独立 asyncio task）
- ✅ 原始 H.264 流输出（.h264 替代 .mp4）
- ✅ H264Parser 单元测试通过
- ✅ **端到端验证通过**（config + 10 帧 H.264 数据，NALU 起始码正确）
- ✅ **scrcpy 二进制控制协议**（ControlSender，输入延迟 <5ms）
- ✅ **自动回退**：控制 socket 不可用时回退到 adb shell input
- ✅ **scrcpy-server 升级到 v4.1**（733706 bytes JAR，协议完全兼容）
- ✅ **禁用视频缩放**：max_size=0 保证帧尺寸 == 设备分辨率（修复坐标映射错位）
- ✅ **动态 codec 提取**：从 SPS NAL 单元解析 profile/level 生成 avc1.XXYYZZ
- ✅ **视频 config 消息携带设备分辨率**：width/height 发送给前端初始化 canvas
- ✅ 自适应码率控制（客户端 fps 上报 → BitrateAdvisor 迟滞决策 → 按档重启编码器，事件驱动 + 重启宽限期 + 静止防误判）
- ⏳ 方案C（直接调用 scrcpy 库）评估

#### 前端视频播放器（95%）
- ✅ VideoPlayer 组件框架
- ✅ H264VideoStream 服务（WebCodecs VideoDecoder）
- ✅ Annex B → AVCC 格式转换
- ✅ avcC description 构建（SPS/PPS 提取，保留 NAL 头）
- ✅ Canvas 渲染（VideoDecoder output → drawImage）
- ✅ 自动模式检测（WebCodecs 支持检测）
- ✅ 截屏模式回退（WebCodecs 不可用时）
- ✅ 坐标映射（InputController，基于 canvas 实际像素尺寸）
- ✅ 输入事件转发（touch/swipe/key/text）
- ✅ 前端构建通过（TypeScript 错误修复）
- ✅ **H264 模式点击修复**：收到 config 后立即初始化 canvas 尺寸（不再等第一帧解码）
- ✅ **binaryType 设置时序修复**：在 connect() 前设置 arraybuffer
- ✅ **消息处理器注册时序修复**：start() 在 connect() 前调用，避免 config 消息丢失
- ✅ **avcC totalSize 修复**：7 + 2 + sps.length + 2 + pps.length（含 numOfPPS 字段）
- ✅ **AVCC SPS/PPS 保留 NAL 头**：不再去除 NAL header，WebCodecs 要求包含
- ✅ **解码输出不覆盖 canvas 尺寸**：保持与设备分辨率一致，确保坐标映射正确
- ✅ **15 秒超时回退**：H264 15 秒未成功流式化则切换到截图模式
- ⏳ 浏览器端到端测试（视频解码 + 输入控制）

#### 集成测试与部署（#8，2026-09-18 地基版完成）
- ✅ 测试治理：重写 `tests/scrcpy/test_encoder.py` + `test_server_manager.py`（14 失败→0），mock 到位、跨平台不依赖真机/子进程；后端单测 155 passed/1 skipped
- ✅ 后端静态检查：`ruff` 清零 + `mypy` strict 全量清零（56 源文件，移除 ignore_errors 白名单）
- ✅ 前端 ESLint 9 flat-config 迁移并清零，lint 纳入构建（不新增依赖）
- ✅ CI/CD：`.github/workflows/ci.yml`（push/PR→master；后端 ruff+mypy+pytest / 前端 eslint+vitest+build；E2E 不进 CI）
- ✅ Docker 降级为开发可选（加未实测 caveat，移出受支持路径）
- ✅ 部署文档：`docs/部署指南.md` + README 端口/命令/结构对齐 + Makefile `ci` 目标
- ⏳ Windows 绿色 exe 打包（下一个独立任务，见优先级 #9）

## 技术风险

| 风险 | 影响 | 概率 | 状态 | 应对 |
|------|------|------|------|------|
| scrcpy-server 集成复杂 | 高 | 中 | 🔴 | 提前 PoC |
| WebCodecs 兼容性 | 中 | 低 | 🟢 | 仅 Chrome |
| ADB 连接不稳定 | 高 | 高 | 🟡 | 自动重连 |
| PTY Windows 支持 | 中 | 高 | 🟢 已化解（2026-09-17 ConPTY 接入，见方案07） | pywinpty |
| 性能瓶颈 | 中 | 中 | 🟡 | 优化 |

## 下一步行动

### 本周（Week 1-2）
1. ✅ 完善 `AdbCliDriver` 错误处理（完成 2026-09-08）
2. ✅ 实现设备状态自动刷新（完成 2026-09-08）
3. ✅ 编写 `DeviceService` 单元测试（完成 2026-09-08，44 个测试）
4. ✅ 验证设备发现功能（完成 2026-09-09，通过页面连接设备）
5. ✅ 实现设备管理 UI（完成 2026-09-09，添加/断开/SSE 实时监听）

### 近期待办（原"下周 Week 3"，2026-09-17 校准）
1. ⏳ 实现端口可配置化（当前硬编码 8765，需要加入 settings 配置支持自定义端口）
2. ✅ ~~修复视频流 MP4 容器解析~~ 已取消：方案A 改为后端直接输出 Annex B 原始流（.h264），无需前端解析 MP4；H.264 端到端已于 2026-09-11 调通（见第十四次更新）
3. ✅ 优化临时文件清理机制（已完成：run_server.py 启动时清理旧文件 + stop() 断开时删除，异常退出场景已覆盖，2026-09-09）

### 当前阶段优先级（2026-09-17 定）：功能实现与体验优化

> 打包 exe 暂缓（见待排期与 `方案/15`），以下功能/体验项完成并达标后再二次评估。

1. 浏览器端到端测试：✅ 已落地 `tests/e2e/`（Playwright，10 用例：9 通过 + 1 合理 skip）——H264 解码/输入控制/截图回退全通过；坐标映射旋转项因设备不支持软件旋转（rk3288 ROM 忽略 user_rotation）走探测 skip。详见 `docs/superpowers/plans/2026-09-17-browser-e2e-testing.md` 与 `tests/e2e/README.md`
2. ✅ ~~调试会话管理补齐~~ 已完成（2026-09-17）：连接池/批量写入/断线续传/会话恢复落地（单测 + E2E 回归通过，真机冒烟验证续传与重启恢复），75% → 95%（余并发锁）。详见 docs/superpowers/plans/2026-09-17-debug-session-management.md
3. ✅ ~~远程 Shell PTY 伪终端~~ 已完成（2026-09-17）：宿主侧 ConPTY（pywinpty 3.x 直连 PTY，chcp 65001 保 UTF-8 全链路，taskkill /T 树清理，use_conpty 开关自动降级管道）；Windows PTY 技术风险随之化解
4. ✅ ~~RK3288 回退体验优化~~ 已完成（2026-09-17）：前端 watchdog 四信号提前感知——error 即时回退 / 码流冻结 3s（stalled，修复"出过前几帧后崩溃永不回退"盲区）/ 无流 8s（no-stream）/ 15s 兜底保留；纯函数 `services/videoFallback.ts` + vitest 10 用例，E2E 正常流零误触发
5. ✅ ~~命令面板（Ctrl+K）~~ 已完成（2026-09-17）：纯函数注册表 `services/commandRegistry.ts`（导航/设备/调试 Tab/视图四类命令 + 打分过滤，vitest 9 用例）+ `CommandPalette.vue` 挂载 App，DebugPanel 经 CustomEvent 接收 Tab 切换；E2E 新增 4 冒烟用例（开关/过滤/执行/无匹配）
6. ✅ ~~视频流自适应码率控制~~ 已完成（2026-09-17）：客户端 1Hz 实测 fps → 每 2s 经 video WS 文本 `{op:'stats',fps}` 上报 → 后端 `BitrateAdvisor` 迟滞决策（窗口 8：坏样本≥6 且 <0.7×target 降一档；满好且距上次切换≥冷却升一档，上限=起始档）→ 事件驱动重启 scrcpy-server 按新档码率出流（协议无运行中改码率，只能重启，见 docs/经验记录.md）。`{type:'restarting'}` 通知前端进 15s 宽限期（暂停 #4 watchdog 与 stats 上报，避免重启黑屏误判/污染决策）；epoch+1 触发 WS 层重置 H264Parser 强制重发 config。静止画面防误判：客户端仅 fps≥1 才上报。测试：后端单测 15（`test_bitrate_advisor`+`test_stream_adaptive`，含静止画面仍秒级重启）、真机冒烟（.22 切档 77ms 生效、重启间隙 ~4.1s→宽限必要）、E2E `APP_ENV=e2e` 关自适应保基线确定（12 passed + 2 已知 .22 ROM 差异，自适应零触发）。commits `3cf22e4`→`21ce82a`
7. ✅ ~~端口可配置化~~ 已完成（2026-09-17）：单一权威变量 `BACKEND_PORT`(+`BACKEND_HOST`)，默认 8765，优先级 env>.env>base.yaml>默认。后端 `get_settings` 显式覆盖 server 节点（base.yaml 作 init kwargs 压过 pydantic env，故不能靠 SettingsConfigDict env 自动映射）；`run_server` 从 settings 读 host/port；vite 代理、playwright、smoke.spec、集成测试统一读该变量；`.env.example` 同步。验证：`tests/unit/test_config_port.py` 6 用例 + 应用层 55 绿、`BACKEND_PORT=9111` 起后端探活 OK、E2E(.22) 基线不变 12P+2 已知 ROM 差异。commit `79555fb`
   - ✅ 已清（#8，2026-09-18）：`tests/scrcpy/test_encoder.py` + `test_server_manager.py` 共 14 例失败根因为测试漂移（mock `ensure_server` 而实现已改 `push_server`），已重写为 mock 到位、不依赖真机/子进程，跨平台可在 ubuntu CI runner 跑（ubuntu CI 首跑又额外清理 3 伪装脚本+2 jar 依赖用例，见 #8）；后端单测基线 152 passed/1 skipped
8. ✅ ~~集成测试与部署（地基版）~~ 已完成（2026-09-18）：测试治理(14→0)/ruff 清零/mypy strict 清零(56 文件)/ESLint9 flat-config 迁移清零/GitHub Actions CI(ruff+mypy+pytest / eslint+vitest+build，push-PR→master)/Docker 降级为开发可选/部署文档(docs/部署指南.md + README 对齐 + make ci 目标)。边界：不含 exe 打包；E2E 不进 CI（需真机，本地 .22 基线 12P+2 已知 ROM 差异）。计划 `docs/superpowers/plans/2026-09-18-integration-testing-ci.md`，commits `8e7010e`→`c123d25`；CI 首跑修复版本漂移/winpty-mypy/pytest 三类 ubuntu 跨环境差异后双 job 全绿（详见 2026-09-18 变更日志）
9. Windows 绿色 exe 打包（下一个独立任务，交付主线）：PyInstaller onedir + 前端同域托管，预估 5~7 人日；调研存档 `方案/15-Windows绿色exe打包方案.md`
10. ✅ ~~设备连接可达性预检优化~~ 已完成（2026-09-18，用户临时优先插入执行）：`adb connect/disconnect` 前置 TCP 可达性预检（`probe_tcp` 1.5s，配置 `adb.probe_timeout`），不可达快速失败不再等 SYN 重传——实测不可达连接 **21.2s→1.55s** 返回 400 `DEVICE_UNREACHABLE`（中文原因直达前端弹窗）；不可达断开跳过 adb 清理直接 200；附带加固 `list_devices` 单设备失败跳过（修复掉线残留表项拖垮列表的既有缺陷）+ 前端断开乐观移除 + connect/disconnect 前端超时对齐(40s)。测试 +7（后端 159 passed/1 skipped）。详见 `方案/16-设备连接可达性预检优化.md`，commits `d1aae9d`→`6159a0b`
11. ✅ ~~H264 断流恢复自动回切 + 截图模式只读徽标~~ 已完成（2026-09-18）：截图模式下探测 ws 码流恢复（3 窗口×2s 各≥4 帧且 3s 内新鲜）→ 自动回切 H264，30s 冷却防横跳；截图模式显示「只读预览·输入不可用」徽标。前端 vitest 24 例。commit `508d0b8`
12. ✅ ~~推流流畅度优化 + 日志按需~~ 已完成（2026-09-18）：5 实施项全部落地并真机验证——1a 解析器 bytes.find 重写 / 2 前端积压丢帧 / 1a 尾 AU 聚帧 / 1b 12 字节头协议转正（.33 十分钟 6236 帧 10.39fps、边界非法 0）/ 3 采样治理 / 5 日志按需 / 4 TCP_NODELAY + Page Visibility；实验落结（降档对照无差异、IDR 两段式实测、静止出帧 ~10fps；经验记录 E020-E022）。详见 `方案/17-推流流畅度优化.md`，commits `001610f`→`6a6f42e`。

### 后续（Week 4-8）
- 按方案文档顺序实施
- 每周回顾进度
- 及时调整计划

### 待实施需求（待排期）

#### 性能/网络数据持久化与下载
- 后端：将 Perf 和 Network 采样数据写入 SQLite（类似 Logcat 的 debug_sessions 表）
- 后端：新增导出 API（CSV/JSON 格式下载）
- 前端：Perf/Network 标签页添加导出按钮
- 注意：需要控制写入频率，避免数据库膨胀（可参考 Logcat 的速率限制策略）

#### Windows 绿色 exe 打包（已排期 → 优先级 #9）
- 结论：可行，推荐 PyInstaller onedir + 前端同域托管，预估 5~7 人日
- 📄 调研存档：`方案/15-Windows绿色exe打包方案.md`
- **决策（2026-09-18）**：#8 地基版（测试/CI/文档）已完成，触发条件达成，本项从「暂缓」升级为**下一个独立任务（优先级 #9）**，作为主交付主线推进

## 质量指标

| 指标 | 目标 | 当前 | 状态 |
|------|------|------|------|
| 单元测试覆盖率 | > 80% | ~30% | 🟡 |
| E2E 测试通过率 | 100% | 100%（9/9，另 1 项设备限制 skip） | 🟢 |
| 代码审查 | 100% | 0% | 🔴 |
| 文档完整性 | 100% | 60% | 🟡 |

## 里程碑

- [x] 项目脚手架搭建（2026-09-08）
- [x] 设备管理功能完成（2026-09-08）✅ 提前完成
- [x] 视频流 PoC 验证（2026-09-08）✅ 方案A 提前完成
- [x] H.264 视频流解析（2026-09-09）✅ NAL 单元解析 + WebCodecs 解码
- [ ] 前端视频播放器完成（预计 2026-09-23）✅ 提前，待端到端验证
- [ ] Logcat 日志系统完成（预计 2026-10-13）
- [ ] MVP 发布（预计 2026-10-27）

## 更新日志

### 2026-09-08
- 完成项目脚手架搭建
- 创建详细实现方案文档
- 建立进度追踪机制

### 2026-09-08（第二次更新）
- ✅ 完善 AdbCliDriver 错误处理（AdbError 异常、超时处理、ADB 未安装检测）
- ✅ 实现 check_connection 连接稳定性检测
- ✅ 实现 connect_tcp / disconnect_tcp TCP/IP 连接支持
- ✅ 实现 DeviceService 后台设备刷新（start/stop/_refresh_loop）
- ✅ 实现设备事件回调机制（on_device_connected / on_device_disconnected）
- ✅ 优化 batch_install 为并发执行（asyncio.gather + Semaphore(5)）
- ✅ 新增 HTTP 端点：/connect、/{device_id}/disconnect、/events（SSE）
- ✅ 编写 44 个单元测试全部通过（test_adb_cli.py 16 + test_device_service.py 28）
- ✅ 整理 scrcpy 复用模块（constants、h264_parser、input_controller、server_manager、encoder）

### 2026-09-08（第三次更新 - 视频流服务方案A）
- ✅ 实现 ScrcpyEncoder（调用 scrcpy.exe 录制视频）
- ✅ 实现临时文件监控和实时数据读取
- ✅ 实现 asyncio.Queue 生产者/消费者模式
- ✅ 实现 StreamService 应用层服务
- ✅ 实现 WebSocket 端点 /ws/video/{device_id}
- ✅ 修复 aiosqlite 线程错误（SqliteDebugRepository 和 SqliteDeviceRepository）
- ✅ 单设备视频流测试通过（68.9秒稳定运行，524336字节）
- ✅ 多设备并发测试通过（两台设备各262192字节）
- ✅ WebSocket 集成测试通过
- ✅ 多设备 WebSocket 测试通过

### 2026-09-09（第四次更新 - 调试面板完善）
- ✅ 修复 Shell/Logcat 消息混合问题（store 统一管理 WS，按消息类型路由）
- ✅ 新增 execShellWs() 方法（Promise 请求-响应模式）
- ✅ 新增 DockPanel 可停靠组件（底部/右侧切换、拖拽调整大小）
- ✅ DebugPanel 增强（停靠位置切换、快捷键 Ctrl+`/Ctrl+L）
- ✅ DeviceDetail 布局自适应（flex-direction 随面板位置切换）
- ✅ ShellView 命令历史（上/下箭头浏览、Ctrl+C 取消、Ctrl+L 清屏）
- ✅ 服务端日志过滤（per-subscriber 过滤条件，减少网络流量）
- ✅ LogcatView 虚拟滚动（vue-virtual-scroller，支持 50K 日志无卡顿）
- ✅ 修复 test_debug_ws.py（循环读取消息直到找到 shell_output）
- ✅ 调试 WebSocket 测试通过（10 秒 5378 条日志，shell 命令正确路由）
- ✅ 服务端过滤测试通过（按 level 过滤后只收到匹配的日志）
- ✅ 日志导出功能（JSON + CSV 格式，支持过滤条件）

### 2026-09-09（第五次更新 - 智能日志管理）
- ✅ **智能日志精简**：按级别过滤（默认只保留 Warning+）
- ✅ **速率限制**：每秒最多 50 条日志，防止日志风暴
- ✅ **自动清理**：每小时检查，删除超过 7 天的日志
- ✅ **容量限制**：数据库超过 2GB 时自动删除最旧日志
- ✅ **多环境配置**：development/production 不同日志策略
- ✅ **前端统计显示**：LogcatView 工具栏显示数据库大小
- ✅ **手动清理 API**：支持手动触发清理和查看统计
- ✅ **测试验证**：默认配置下日志从 1109 条/秒降到 0 条/秒（无问题时）

### 2026-09-09（第六次更新 - H.264 视频流实现）
- ✅ **后端 H.264 帧解析**：使用 H264Parser 解析 Annex B 格式的 NAL 单元
- ✅ **SPS/PPS 配置提取**：从 H.264 流中提取编解码器配置并发送给客户端
- ✅ **视频帧二进制转发**：IDR 关键帧和 P/B 帧以二进制消息发送
- ✅ **并发输入处理**：重构 video.py 使用独立 asyncio task 处理客户端输入（替代 1ms 轮询）
- ✅ **原始 H.264 流输出**：ScrcpyEncoder 临时文件扩展名改为 .h264（从 MP4 容器改为 Annex B 流）
- ✅ **前端 WebCodecs 解码**：H264VideoStream 服务使用 VideoDecoder 解码 H.264 帧
- ✅ **Annex B → AVCC 转换**：前端将 Annex B 格式转为 AVCC 格式供 VideoDecoder 使用
- ✅ **avcC description 构建**：从 SPS/PPS 构建 avcC 格式的解码器配置
- ✅ **自动模式检测**：VideoPlayer 检测 WebCodecs 支持，不支持时回退到截屏模式
- ✅ **模式状态显示**：控制栏显示当前模式（h264/screenshot）
- ✅ **H264Parser 单元测试**：9 个测试用例验证解析正确性

### 2026-09-09（第七次更新 - 设备管理 UI 完善 + Windows 兼容性修复）
- ✅ **添加设备对话框**：Dashboard 页面新增"添加设备"按钮，弹窗输入 IP 和端口
- ✅ **IP 地址校验**：表单验证 IP 格式（正则校验）
- ✅ **断开设备功能**：TCP/IP 设备显示"断开"按钮，调用 disconnect API
- ✅ **TCP/IP 设备识别**：根据 device_id 格式（ip:port）自动判断是否为无线设备
- ✅ **SSE 实时监听**：设备连接/断开时自动更新列表，无需手动刷新
- ✅ **空状态引导**：无设备时显示提示信息，引导用户添加设备
- ✅ **前端 API 封装**：api.ts 新增 connectDevice/disconnectDevice 方法
- ✅ **设备 Store 增强**：device.ts 新增连接/断开/SSE 管理方法
- ✅ **Windows 事件循环修复**：创建 run_server.py 启动脚本，设置 ProactorEventLoop 策略解决子进程问题
- ✅ **设备连接测试通过**：成功从页面连接 192.168.8.34 设备，无需手动执行 adb 命令

### 2026-09-09（第八次更新 - 端口配置 + 视频流修复）
- ✅ **端口配置优化**：后端服务端口改为 8765（不常用端口，避免冲突）
- ✅ **启动时清理临时文件**：run_server.py 启动时自动清理旧的临时视频文件
- ✅ **scrcpy 录制格式修复**：添加 `--record-format mp4` 参数，解决 scrcpy 4.1 录制失败问题
- ✅ **视频文件清理机制**：stop() 方法中已有清理逻辑，启动时也会清理旧文件
- ✅ **视频流 MP4 容器解析**：~~待实现~~ 已取消——改为后端直接输出 Annex B 原始流（.h264），H.264 端到端已于 2026-09-11 调通（见第十四次更新；2026-09-17 校准）
- ⏳ **端口可配置化**：需要将端口作为配置项加入 settings.py（计划中）

**注意**（2026-09-17 校准：以下描述已过时，H.264 实时流已可用，截屏模式仅作 15 秒超时回退）：
- 当前视频流功能使用截屏模式作为临时方案（~2 FPS），H.264 实时流需要实现 MP4 容器解析
- 临时视频文件会在断开连接时自动删除，启动时也会清理旧文件

### 2026-09-09（第九次更新 - 流式 Shell 输出）
- ✅ **AdbDriver.shell_stream() 协议**：新增 `AsyncIterator[str]` 流式执行接口
- ✅ **AdbCliDriver.shell_stream() 实现**：基于 `asyncio.create_subprocess_exec` + `readline()` 逐行产出
- ✅ **DebugService.exec_shell_stream()**：应用层流式执行 + 自动保存到 shell_history
- ✅ **WS handler 流式发送**：exec 操作改为逐行发送 `shell_stream` 消息，完成后发送 `shell_output`
- ✅ **debug store execShellWs() 增强**：新增 `streamCallback` 参数，`shell_stream` 消息实时传递给调用方
- ✅ **ShellView 流式终端**：每行输出立即写入 xterm.js 终端（无需等待命令完成）
- ✅ **端到端测试通过**：创建会话 → 连接 WS → 订阅 → 执行命令 → 10 行流式输出 → shell_output(success=True) → 关闭会话
- ✅ **Windows 兼容性**：ProactorEventLoop 策略已在 run_server.py 中设置

**协议变更**：
- `shell_stream`: `{ "type": "shell_stream", "line": "..." }` — 命令输出一行
- `shell_output`: `{ "type": "shell_output", "output": "", "success": true, "done": true }` — 命令完成

---

### 2026-09-10（第十次更新 - 视频流端到端验证）
- ✅ **视频流端到端测试通过**：config 消息 + 10 帧 H.264 二进制数据成功传输
- ✅ **修复测试超时**：`test_video_stream.py` 超时从 2s 提升到 5s（视频流启动需要 ~3s）
- ✅ **前端 TypeScript 错误修复**：
  - `tsconfig.node.json`：`allowImportingTsExtensions` 需要配合 `emitDeclarationOnly`
  - `videoStream.ts`：移除未使用的 `ws` 字段
  - `webtransport.ts`：移除过时的 `@ts-expect-error`
  - `LogcatView.vue`：`RecycleScroller` 类型改用 `any`
- ✅ **前端构建成功**：无 TypeScript 错误，产物大小正常
- ⏳ **待完成**：浏览器端到端测试（视频解码显示 + 输入控制）

---

### 2026-09-10（第十一次更新 - 视频播放器诊断与修复）
- ✅ **视频播放器问题诊断**：浏览器显示"正在连接..."的根因分析
  - ✅ 后端 WebSocket 代理验证：Vite 代理正确转发 JSON + 二进制帧
  - ✅ 后端编码器诊断：scrcpy-server 正常启动并完成握手
  - ✅ **根因定位**：Rockchip RK3288 硬件编码器 (OMX.rk.video_encoder.avc) 在发送初始帧后崩溃
  - ✅ 设备日志确认：`OMXNodeInstance: !!! Observer died` 导致编码器停止产出帧
  - ✅ 多种参数测试（降低分辨率/帧率/码率）均无法解决——是硬件层面的兼容性问题
  - ✅ 参考 scrcpy GitHub #2964：编码器停止产出帧是已知设备兼容性问题
- ✅ **H.264 → 截图自动回退**：VideoPlayer.vue 新增 10 秒超时检测
  - ✅ 如果 H264 模式 10 秒内未收到帧，自动切换到截图模式
  - ✅ 截图模式 (~1 FPS) 作为可靠回退方案
  - ✅ 保持 WebSocket 连接用于输入事件
- ✅ **编码器诊断日志**：ScrcpyEncoder 增加 socket_data_received / yielding_data 跟踪
- ⏳ **已知限制**：RK3288 设备的 H.264 实时流不可用，仅能使用截图模式 (~1 FPS)

---

### 2026-09-11（第十二次更新 - 坐标映射修复）
- ✅ **截图模式点击定位修复**：InputController 坐标映射逻辑优化
  - ✅ **问题诊断**：`mapToScreen()` 使用 `deviceWidth/deviceHeight`（设备物理分辨率）映射坐标，但截图模式下 canvas 尺寸与设备物理分辨率可能不一致（如设备横屏）
  - ✅ **根因**：坐标映射应基于**当前显示的视频/截图实际尺寸**，而非设备物理分辨率
  - ✅ **参考 scrcpy 源码**：`app/src/input_manager.c` + `screen.h` 中的坐标转换实现
  - ✅ **修复方案**：使用 `canvas.width/height`（视频/截图实际像素尺寸）进行坐标映射
  - ✅ **边界检查**：添加坐标 clamp 防止超出范围
  - ✅ **HiDPI 支持**：考虑 canvas 实际像素与 CSS 像素的差异
- ⏳ **待验证**：需要在不同设备方向（横屏/竖屏）下测试点击定位准确性

---

### 2026-09-11（第十三次更新 - scrcpy 二进制控制协议重构）
- ✅ **输入延迟优化**：从 `adb shell input`（50-200ms）重构为 scrcpy 二进制控制协议（<5ms）
- ✅ **新建 `ControlSender`**（`backend/app/scrcpy/control_sender.py`）
  - ✅ 完整实现 scrcpy v2.4 二进制控制协议（参考 py-scrcpy-client + scrcpy-server 源码）
  - ✅ 触摸事件（32 字节）：`struct.pack(">BqiiHHHii", ...)`
  - ✅ 按键事件（13 字节）：`struct.pack(">Biii", ...)`
  - ✅ 文本输入（5+text 字节）
  - ✅ 滚动事件（21 字节）
  - ✅ asyncio.Lock + drain timeout 保护并发写入
- ✅ **重构 `ScrcpyEncoder`**（`backend/app/infrastructure/stream/scrcpy.py`）
  - ✅ 建立控制 socket 并创建 ControlSender 实例
  - ✅ 新增 `send_input(data: dict)` 方法（支持 touch/swipe/key/text）
  - ✅ 滑动实现：DOWN → N×MOVE → UP（线性插值，最大 100 步）
  - ✅ 控制 socket 不可用时自动回退到 `adb shell input`
  - ✅ 新增 `resolution` 属性（协议握手时读取屏幕分辨率）
  - ✅ 清理过度调试日志
- ✅ **重构 `video.py`**：`_handle_input()` 改为调用 `encoder.send_input(data)`
- ✅ **重构 `stream_service.py`**：新增 `get_encoder(device_id)` 方法
- ✅ **清理旧代码**：删除 `input_controller.py` 及其测试文件
- ✅ **更新导出**：`__init__.py` 导出 ControlSender，ScrcpyEncoder 指向 infrastructure 新版
- ✅ **验证**：Python 导入正常，109 个测试通过，TypeScript 编译通过
- 📄 **方案文档**：`方案/13-scrcpy二进制控制协议重构.md`（含协议格式验证、风险分析、实施清单）

---

### 2026-09-11（第十四次更新 - 视频流 H264 端到端调通 + 点击控制修复）
- ✅ **AVCC description 构建修复**：
  - ✅ SPS 索引修正：`sps[1]/sps[2]/sps[3]`（跳过 NAL 头）正确提取 profile/compatibility/level
  - ✅ `totalSize` 计算修复：`7 + 2 + sps.length + 2 + pps.length`（原公式缺少 numOfPPS 1 字节字段）
  - ✅ AVCC SPS/PPS 保留 NAL 头：`stripStartCode()` 只去起始码，不去 NAL header（WebCodecs 要求）
- ✅ **消息处理器时序修复**：`h264Stream.start()` 在 `ws.connect()` 前调用，避免 config 消息在处理器注册前到达被丢弃
- ✅ **binaryType 设置时序修复**：`binaryType = 'arraybuffer'` 在 `connect()` 方法内立即设置
- ✅ **scrcpy-server 升级到 v4.1**：JAR 大小 733706 bytes，协议完全兼容
- ✅ **后端 codec 动态提取**：从 SPS NAL 单元解析 profile_idc/constraint_flags/level_idc，生成 `avc1.XXYYZZ`
- ✅ **后端 config 消息携带设备分辨率**：`width/height` 从 `encoder.resolution` 获取并发送
- ✅ **H264 模式点击控制修复**：
  - ✅ 收到 config 后立即设置 canvas 尺寸为设备分辨率（不再依赖第一帧解码才更新）
  - ✅ 解码输出回调不再覆盖 canvas 尺寸（保持坐标映射一致性）
  - ✅ `max_size=0` 禁用视频缩放：保证视频帧尺寸 == 设备物理分辨率（消除坐标缩放错位）
  - ✅ 15 秒超时回退：H264 模式 15 秒未成功流式化则切换到截图模式
- 📄 **经验记录**：`docs/经验记录.md` 新增 E011-E015（WebCodecs AVCC、消息时序、坐标映射等）

---

### 2026-09-11（第十五次更新 - 调试面板其他标签完善 Perf / Apps / Network）

- ✅ **Perf 性能监控标签**
  - ✅ 后端基础设施层：`PerformanceSampler`（CPU/MEM/FPS/Activity 采集）
    - CPU：基于 `/proc/stat` 两次采样差值计算
    - 内存：解析 `/proc/meminfo`（MemTotal - MemAvailable）
    - FPS：`dumpsys gfxinfo` 累计帧差值计算实时帧率
    - Activity：`dumpsys activity activities` 提取前台包名
  - ✅ 后端应用层：`PerformanceService`（内存环形缓冲 + pub/sub）
  - ✅ 后端接口层：HTTP `/api/perf/{device_id}/metrics|start|stop` + WS `/ws/perf/{device_id}`
  - ✅ 前端：`PerfView.vue`（实时指标卡片 + ECharts 折线图 + 60 秒历史）

- ✅ **Apps 应用管理标签**
  - ✅ 后端应用层：`AppService`（列表/详情/启动/停止/卸载/清数据/内存）
  - ✅ 后端接口层：HTTP `/api/apps/{device_id}` RESTful 端点
  - ✅ 前端：`AppsView.vue`（应用列表 + 搜索筛选 + 操作按钮 + 二次确认对话框）

- ✅ **Network 网络监控标签**
  - ✅ 后端基础设施层：`NetworkSampler`（流量/连接/WiFi 采集）
    - 流量：`/proc/net/dev` 读取 wlan0 收发字节
    - 连接：解析 `/proc/net/tcp` + `/proc/net/udp`（hex 地址转换）
    - WiFi：`dumpsys wifi` 提取 SSID 和状态
  - ✅ 后端接口层：HTTP `/api/network/{device_id}/stats|connections`
  - ✅ 前端：`NetworkView.vue`（WiFi 状态 + 流量卡片 + ECharts 曲线 + 连接列表）

- ✅ **前端集成**
  - ✅ `DebugPanel.vue` 更新：导入并渲染 5 个标签页组件
  - ✅ `api.ts` 扩展：perf/apps/network 三组 API 方法
  - ✅ TypeScript 编译通过

- 📄 **方案文档**：`方案/14-调试面板其他标签完善.md`

---

### 2026-09-11（第十六次更新 - 调试功能优化）
- ✅ **Logcat 录制暂停/恢复**：Chrome DevTools 风格的录制控制
- ✅ **日志搜索增强**：支持 Tag + Message 模糊匹配
- ✅ **视频画面自适应**：`object-fit: contain` 按容器比例显示
- ✅ **网络流量采样器实例缓存**：修复速率计算错误
- ✅ **性能 FPS 修复**：解决应用切换时帧率出现负值的问题
- ✅ **日志清理按钮**：支持清除当前会话日志

---

### 2026-09-17（文档校准）
- 修正「性能监控（0%）待开始」→ 移入已完成（实际 95%，见第十五次更新）
- 移除过时的「MP4 容器解析」待办（方案已改为 Annex B 原始流，端到端已调通）
- 补记第十六次更新条目（commit 5f2b92f，此前缺失）
- 新增待排期需求：性能/网络数据持久化与下载
- 实测验证：后端/前端启动正常，46/47 测试通过（1 个需 ADB 真机），vue-tsc 0 错误
- 完成 Windows 绿色 exe 打包可行性调研（推荐 PyInstaller onedir，5~7 人日）；**决策：暂缓执行**，优先当前版本功能实现与体验优化，达标后二次评估。存档 `方案/15-Windows绿色exe打包方案.md`

---

### 2026-09-18（第十七次更新 - 集成测试与部署·地基版 #8）
- ✅ **测试治理**：`tests/scrcpy/test_encoder.py` + `test_server_manager.py` 共 14 例失败清零——根因为测试漂移（mock `ensure_server` 而实现已改 `push_server`），重写为 mock 到位、不依赖真机/子进程；后端单测基线 **152 passed / 1 skipped**（ubuntu CI 首跑又暴露 5 例跨环境失败，见下「CI 首跑转绿」）
- ✅ **后端静态检查**：`ruff check backend/app` 清零；`mypy backend/app` strict 全量清零（56 源文件，**移除 ignore_errors 白名单**；含消除重复 `EncoderOpts`、Starlette handler 收窄 `type: ignore` 等）
- ✅ **前端 ESLint 9**：flat-config 迁移（`@vue/eslint-config-typescript@13` 无扁平导出，手工组合 `@typescript-eslint` v7 + `eslint-plugin-vue`，不新增依赖），`eslint .` 清零并纳入构建
- ✅ **CI**：新增 `.github/workflows/ci.yml`（push/PR→master，ubuntu-latest；后端 ruff+mypy+pytest，前端 eslint+vitest+build；**E2E 不进 CI**，需真机本地跑）
- ✅ **CI 首跑转绿**：首次 push 在 ubuntu 暴露两处跨环境差异并已修，现 frontend/backend **双 job 全绿**：① 门禁版本漂移——`pip install` 浮动版 ruff 规则集扩大报 BLE001、`@typescript-eslint@7` 与 `eslint@9` peer 冲突致 `npm ci` ERESOLVE → 锁 ruff/mypy/pytest 到本地版 + `npm ci --legacy-peer-deps`（commit `2cdf22c`）；② mypy `winpty.py` 直引 `subprocess.CREATE_NO_WINDOW`（typeshed 仅 Windows stub 有）在 Linux 报 attr-defined → 改 `getattr` 兜底（commit `060cbbf`）；③ pytest 5 例：3 个带 `test_` 前缀、无断言的真机脚本(`test_minimal_scrcpy/test_scrcpy_direct/test_scrcpy_server`)实际调 adb → 更名 `debug_*.py` 退出收集，2 个 `ensure_server` 用例读磁盘上被 gitignore 的 `scrcpy-server.jar` → 改 `tmp_path` 造假 jar（commit `c123d25`）
- ✅ **Docker 降级**：三文件加 `[注意]` 未实测 caveat，移出受支持发布路径（依赖 Windows 专属组件）
- ✅ **部署文档**：新增 `docs/部署指南.md`；README 端口(8765/8080)/命令/结构对齐；Makefile 新增 `ci` 目标并修 `test`/`lint`
- 边界：本次为「地基版」，**不含 Windows 绿色 exe 打包**，顺延为下一个独立任务（优先级 #9）
- 计划 `docs/superpowers/plans/2026-09-18-integration-testing-ci.md`；commits `8e7010e`→`c123d25`（含 CI 首跑转绿修复 `2cdf22c` 版本锁/`060cbbf` winpty-mypy/`c123d25` pytest 治理）

---

### 2026-09-18（第十八次更新 - 设备连接可达性预检 #16）
- ✅ **预检模块**：新建 `infrastructure/adb/reachability.py`（`probe_tcp`：超时/拒绝/DNS/OSError 四类失败映射中文原因）；新增 `DeviceUnreachableError`（→400 `DEVICE_UNREACHABLE`）与配置 `adb.probe_timeout: 1.5`
- ✅ **驱动层**：`connect_tcp` 前置预检不可达即抛（不进入 adb 子进程）；`disconnect_tcp` 不可达跳过 adb 清理直接返回
- ✅ **列表加固**（设计走查新增）：`list_devices` 单设备 `get_device_info` 失败跳过+告警，修复掉线残留表项致整个列表 400 的既有缺陷
- ✅ **前端**：`extractApiError` 读结构化 `error.message`（不可达中文原因直达弹窗）；connect/disconnect 超时 40s（> 后端 adb.timeout 30s）；断开成功乐观移除行再重同步
- ✅ **手工验证**：不可达连接 **21.2s→1.55s** 返回 400（`.99` 对照基线）；可达连接/断开（`.22`）正常；不可达断开 1.51s 返回 200 且跳过 adb（日志 `tcp_disconnect_skipped_unreachable`）；设备列表正常
- ✅ **门禁全绿**：后端 ruff 清零 / mypy strict 57 文件 / pytest **159 passed + 1 skipped**（基线 152，+7 新例）；前端 eslint 清零 / vitest 19 passed / build ✓
- 方案：`方案/16-设备连接可达性预检优化.md`；经验记录新增 E019（adb connect 不可达需等 SYN 重传；本机回环关闭端口 RST 延迟 ~2s 影响测试超时取值）；commits `d1aae9d`→`6159a0b`

---

### 2026-09-18（第十九次更新 - H264 回切 #52 + 推流流畅度调研 #17/日志按需）

- ✅ **H264 断流恢复自动回切**：截图模式下周期性探测 ws 码流（3 窗口×2s 各≥4 帧且最新 3s 内）→ 自动回切 H264；30s 冷却防「恢复几秒又卡死」横跳；`h264VideoStream` 新增 suspend（保留 ws 处理器只计数不解码）/resume（暂存 config 重建解码器）；回切后重新武装 watchdog。前端 vitest **24 passed**、eslint 清零、build ✓。commit `508d0b8`
- ✅ **截图模式只读徽标**：截图模式 streaming 时左上角显示「只读预览 · 输入不可用」，默认隐藏。commit `508d0b8`
- ✅ **推流流畅度调研完成**（方案 `方案/17-推流流畅度优化.md`，commit `faf90a3`）：
  - 8 项根因：Rockchip 硬编码器故障(0.4fps，无软件根治靠回退+回切兜底)/H264Parser 逐字节扫描（benchmark 4.6MB/s vs bytes.find 对照 **885MB/s**，4Mbps 一路占 ~11% 单核）/前端解码队列无积压丢帧/perf 采样每轮 5 个 adb 子进程（含 2 次重复 dumpsys，日志实锤）/Wi-Fi 信道共享/后台标签页解码争抢/截图模式固定 1.5s 轮询/日志无条件采集
  - 5 个实施项：① 解析器重写 bytes.find+收缩缓冲（含随机流对拍测试）② 前端 decodeQueueSize 积压丢 delta 帧 ③ sampler 去重+并发+降频+失联停采 ④ **日志按需**（后端 create_session 不再无条件启动 logcat 采集，首个 paused=false 订阅者惰性启动、全暂停后停采；前端 LogcatView 挂载不再无条件 ws+fetchLogs，仅开启录制才拉取并滚动显示，断线重连加 isRecording 门）⑤ TCP_NODELAY/Rockchip 降档实验/Page Visibility
  - 待评审后按序实施

---

### 2026-09-18（第二十次更新 - 方案 17 实施：1a/2/1a尾/1b/3/5/4 代码部分全部提交）

- ✅ **实施项 1a**（`001610f`）：H264Parser 起始码扫描改为 `bytes.find` + 回看修正（848B/s → 885MB/s，4Mbps 一路 ~11% 单核占用消除）；含随机流对拍测试
- ✅ **实施项 2**（`7fd2804`）：前端解码路径治理——单遍扫描（extractNalus 产出复用）、`decodeQueueSize` 积压丢 delta 帧保 key、跨 IDR 超阈重建解码器、日志收敛
- ✅ **实施项 1a 尾步骤**（`12e3a0f`）：AU 聚帧发送（纯函数 `aggregate_aus` + 单测；多 slice 编码器不被误切）
- ✅ **实施项 1b**（`5e07963`）：恢复 scrcpy-server 12 字节包头协议——按 pack 边界切包根治解析开销，前端零改动；**真机冒烟转正待执行**（见下）
- ✅ **实施项 3**（`39ca680`）：perf 采样治理——activity/cpu/mem 并发（5→4 次 shell/轮）、gfxinfo 降频（5 轮 1 采）、失联 5 次停采、shell 8s 超时兜底；7+1 例新测试
- ✅ **实施项 5**（`b66a547` 后端 + `d9674d4` 前端）：日志按需——后端 logcat 惰性采集（首个 paused=false 订阅者启动、全暂停 500ms 宽限后停采、任务自清理）；前端 LogcatView 挂载零网络请求，开启录制才连 WS/拉历史/拉统计，断线重连加 isRecording 门；13 例测试（后端 10 + 前端 vitest 7，新增 @vue/test-utils + happy-dom 基础设施）
- ✅ **实施项 4 代码部分**（`ee6f850` + `1c1857f`）：TCP_NODELAY 顺手项（uvicorn sansio 协议实例 transport 取 socket，Proactor 回退 pipe，best-effort）；Page Visibility 降载（tab 隐藏停 fps 上报防决策污染 + InputController.setEnabled 停输入，恢复可见重启）；`?swdecode=1` 强制 `prefer-software` 实验开关（默认不启用）；8 例测试
- ⏳ **待真机执行（需设备 .18/.22 在场）**：
  1. 1b 冒烟转正：两机各 10 分钟推流观察（掉线/花屏/AU 帧序）
  2. AU 聚帧帧序验证：各 30s 抓包比对包边界
  3. Rockchip 降档实验（.18 专项）：fps 30/bit_rate 2M 下 vpu 故障复现率对照；base.yaml 临时改参，实验后回退，结论按经验记录流程落 E0xx
  4. repeat-previous-frame 行为确认：静止画面实测 fps 是否 ~10（scrcpy 官方 100ms 重复帧），核对与 VideoPlayer 注释「静止 fps≈0」的差异并记录事实
  5. prefer-software 对照：.18 花屏时 `?swdecode=1` 前后对比
  6. Page Visibility 实测收益：后台 tab 时设备端编码负载/带宽变化
- 门禁全绿：后端 pytest **219 passed + 1 skipped**（基线 214，+5 新例）/ ruff 清零 / mypy strict 60 文件；前端 vitest **58 passed**（基线 50，+8）/ eslint 清零 / build ✓
- 方案：`方案/17-推流流畅度优化.md`；本地提交领先远端，待推送授权

---

### 2026-09-18（第二十一次更新 - 方案 17 真机验证执行完成：1b 转正 + 实验项全部落结）

- ✅ **1b 冒烟转正通过**（.18/.33 各 10 分钟推流观察，抓包脚本 `tests/manual/capture_ws_frames.py`，commit `48a0ee8`）：
  - .33：十分钟 6236 帧（10.39fps，零帧秒 0）；起始码边界非法 0、config×1、error 0、无掉线花屏
  - .18：十分钟 241 帧（0.40fps，44/600 秒有帧）——vpu 硬编码器故障复现，属已知问题；边界非法 0、config×1
  - **结论：`raw_stream_fallback` 保持默认 false（1b 转正）；1a 起始码兜底路径保留**，.18 靠 watchdog 截图回退 + 探测自动回切兜底
- ✅ **AU 聚帧帧序验证**：30s 抓包比对包边界无异常（转正随 1b 冒烟一并覆盖）
- ✅ **降档实验：故障与码率无关**：.18 同窗口对照 4M vs 2M——帧数 66 vs 65、有帧秒 12 vs 12 完全一致（总字节仅 -36%）→ 补 E009 的 rk3568 未验证缺口；base.yaml 临时改参已回退（git diff 为空）
- ✅ **repeat-previous-frame 静止出帧事实确认**：.33 静止/关屏仍 ~8-10fps，89.8% 帧 ≤100B（p50=15B 近空 P 帧）；「静止 fps≈0」注释口径不成立，但上报 gate（measured<1）在两种情形均安全；另实测 **IDR 两段式周期**（启动 60 帧内每 5 帧一个 → 稳态 42-57s，设备端行为非本仓参数）
- ✅ **prefer-software 对照**（.18）：前端实测解码 21 帧无花屏无解码错误；`?swdecode=1` 真机透传生效，开关前后无差异可观测；开关保留备用
- ✅ **Page Visibility 实测**：tab 隐藏后前端停发 fps 上报、输入停用实测生效（headed 模式验证）；恢复路径已由单测覆盖，真机复验受 .33 启动抖动干扰未闭环（见下）
- ✅ **经验记录 E020/E021/E022 落盘**：编码器静止出帧与 IDR 周期实测（RK3566）/ rk3568 间歇出帧故障与码率档位无关 / 视频流每次连接完整启停 scrcpy-server（push jar 耗时抖动 1s→10.1s，引发 no-stream 误回退闪变）
- ⏳ **待用户在场复核**：① Page Visibility 恢复路径真机闭环 ② .33 多次连接后启动变慢观察项（首帧 4.3s→14.1s，已停止高频实验避免放大设备负载）
- ✅ **文档回写完成**：`方案/17-推流流畅度优化.md`（状态行 + 2.4 节实验结论）、`docs/经验记录.md`（E020-E022）、本文件第二十一次更新
- 门禁：本批仅文档/脚本变更，代码未动；门禁维持转绿（后端 pytest 219 passed + 1 skipped / ruff 清零 / mypy strict；前端 vitest 58 passed / eslint 清零 / build ✓）
- 方案：`方案/17-推流流畅度优化.md`；本地提交领先远端，待推送授权

---

**最后更新**：2026-09-18（第二十一次更新 - 方案 17 真机验证完成：1b 转正 + 实验落结 + 文档回写）
**下次回顾**：2026-09-25
